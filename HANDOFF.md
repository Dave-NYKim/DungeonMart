# Dungeon Mart 인계 노트

## 2026-09-21 · 보스 골드90% 감소·장비 판매가 표시

- 후속 요청: 보스가 주는 돈이 과다함(일반 사냥/판매는 대상 아님), 지급 대상 설명 및 장비별 가격 표시.
- 구현: 액트 보스 운영금 노멀25/40/55/70G, 최종보스 운영금200G. 난이도 보상3/8배 유지. 최종보스 참여 용사 개인 골드도 floor(zoneStats.gold×2.5)로90% 감소(노멀1=162G/명). 액트 보스는 개인 골드 없음. 일반 사냥·장비판매·기존 잔액 유지. 소환500G와 시설 비용 유지, 최종보스전은 운영금 순수익 목적이 아닌 진행/전리품 목적.
- UI 원인: 가격이 창고 설명 말미의 ellipsis에 가려져 있었음. 목록/상세에서 별도13px 금색 판매가 줄로 분리, 구매 장비는 구매 완료. 용사별 가격 보정 일치, 상세 구매 버튼에 실제 결제액 표시. 새 입력이나 저장 구조 변경 없음.
- 변경: src/engine.js, src/gear-ui.js, style.css, tests/campaign.test.mjs, tests/equipment-browser-smoke.mjs, README.md 및 기록 문서.
- 검증: npm test65/65. 보스 종류/3난이도별 운영금과 개인 골드·비참여자 미지급 검증. 격리 모바일390×844/360×640 실제 터치 창고/제작소 가격·구매완료·13px·넘침 없음 통과. 기존액트/성장 검증 포함.
- 배포: 이전 게임 변경 커밋c63ea3a와 함께 main/Pages 배포 예정. 다음: 실기기 성장/운영금 경제 체감 조정, 초기 이미지 전송 최적화.

## 2026-09-21 · 단계 경계 강화·최대100·경험치 곡선

- 요청: 다음 단계 ACT1이 이전 ACT4보다 강하게, 전직 무관 최대100, 10까지 기존 성장·이후 어렵게·40부터 극악. 레벨당 스탯 증가 유지. 앞서 승인된 서버 배포에 포함.
- 구현: 전체30단계 매 단계 전투배율5배(노멀1=1, 나이트1=5^10, 헬1=5^20). 일반 ACT1 HP/공격은 이전 ACT4의 약2.27/2.63배, 액트 보스는1.25/2.19배. 최종보스도 동일 배율. 보상 배율은 기존 유지. 고단계는 현재 장비로 달성 가능하도록 맞춘 목표가 아니며 매우 높음.
- 용사 최대100, 전직20/40 조건 유지. Lv1~9의 다음 레벨 XP는 기존 공식 floor(35+17L+3L^1.45). Lv10부터 ×4×1.22^(min(L,39)-10), Lv40부터 추가 ×12×1.24^(L-39). Lv9→10=260, 10→11=1,158, 39→40=1,669,398, 40→41=25,596,468, 99→100=25,130,388,815,826. 기존 레벨·XP 수치 그대로 보존,100 도달 시 잉여XP0. statsOf의 레벨당 atk3.3/def1.3/hp17 및 기존 등급/장비 적용은 변경 없음.
- 저장: combatRevision2로 기존 적 능력치 갱신, HP 비율 보존, 액트 보스별 배율 유지. 기존 재화·아이템·용사 저장 보존. 최대레벨 검증100으로 확대.
- 모바일: 큰 숫자는1e9부터 지수표기로 축약, 난이도/보스HP 긴 숫자 넘침 방지. 최대레벨 설명100으로 수정.
- 변경: src/{data,engine,app}.js, README.md, tests/engine.test.mjs, tests/progression-balance.test.mjs, tests/campaign-browser-smoke.mjs 및 기록 문서.
- 검증: npm test 64/64 통과. 전체29개 경계·일반/액트보스 비교, XP곡선/스탯증분/100 저장, 구전투 저장 마이그레이션 검증. 격리 Whale9223 모바일390×844·360×640 터치 액트/최종 소환 및 헬10 수치 넘침 없음. git diff --check 통과.
- 배포: 승인된 main/Pages 배포 진행. 다음: 실제 휴대폰 장기 성장/전투 피드백과 이미지 초기전송 최적화.

## 2026-09-21 · 현대 편의점·액트 보스 진행·북쪽 최종 결전장

- 요청: 마을 현대 편의점, 액트 건물 외형/기능, 액트1 보스→2→3→4→최종보스 소환, 최종보스 처치 시 다음 난이도 단계. 모든 헌터 보스 집결. 후속으로 서버 배포 명시 요청.
- 그래픽: assets/buildings/sprites.png 투명 3×2 시트(현대 MART, 지하묘지/왕릉/덩굴사원/용암문, 붉은 봉인석 결전장), assets/act-bosses/sprites.png 4종. src/campaign-art.js 로드/캐시와 scene 재빌드 연결. 북쪽 종말의 봉인진(1808,416)과 연결 도로/보행 영역 개방, 결전장은 바닥 레이어. 기존 최종보스 픽셀은 유지, 액트 보스는 전용128px.
- 진행: campaign.clears['tier:stage']=0~4 저장. 레벨 해금 제거, 앞 액트 보스 처치로 다음 액트 해금. 액트4 처치 후에만 summonBoss 가능. 최종보스만 다음 단계 해금(노멀10→나이트1→헬1 규칙 유지). 새 단계는 ACT1부터, 이전 단계 처치 기록 보존. 액트 보스 재도전 가능, 별도 입장료 없음.
- 액트 보스: 묘역의 파수장/황금 무덤의 황제/고대 가시왕/용암문의 집행자. 최종보스 기준 HP .18/.32/.5/.72, 공격 .35/.5/.65/.8, 각 스킬 범위/주기/명칭과 지역 부하 구분. 액트 처치 보상 운영금250/400/550/700×난이도 보상, 참여 XP/영혼. 기존 final 보상·500G 소환·쿨다운 유지.
- UI: 사냥터 액트 보스 도전 버튼 및 건물 탭 도전, 잠금 조건/목표 문구 수정. 오아시스 탭은 선택 용사가 실제 이동 후 무료 회복. 최종 소환 버튼과 북쪽 결전장 탭 모두 동일 gate 적용. 실제 터치 가능44px.
- 집결: 살아 있는 헌터는 사냥/귀환/회복/대기 모두 현재 위치에서 보스 쪽으로 이동. 보스전 중 저체력 자동귀환 중지, 신규 영입도 합류. 영혼은 회복소 부활 후 합류. 배 위 입장 중인 용사는 항구 등록소에서 출발. 원래 액트/마을대기 배정은 종료 후 복귀. 보스전 동시1개, 제한4게임분.
- 저장: 기존 campaign 없는 저장은 bossKills>0이면 현재 단계 액트4 통과 인정, 아니면0부터; 재화·장비·용사·기존 난이도 해금 보존. 잠긴 지역 배정은 ACT1로 보정. 기존 진행 중 final은 북쪽으로 이동해 집결 재개. 새 데이터 유효성 검사. world revision은 유지(기존 마을 배치 보존).
- 검증: 전체 npm test 61개 통과. campaign.test 4개(순서 해금/최종 gate/다음단계/저장/북쪽 경로/여러 상태 집결/오아시스) 추가. 기존 최종보스 테스트는 액트4 완료 fixture와 북쪽 위치로 갱신. 무장비 Lv60은 여전히 나이트1 final 처치 실패(위치/귀환 변경으로 정확한 잔여HP 비율 assertion은 생존 여부로 변경).
- 격리 Whale 9223 tests/campaign-browser-smoke.mjs 390×844·360×640 실제 터치 액트 보스/최종 소환·집결·투명 건물·넘침/오류 검사 통과. 몬스터/기존 제작·창고·도감 모바일 스모크 통과. 화면 /tmp/dungeonmart-review/{modern-mart,act-one-boss,northern-final-boss}-390.png.
- 배포: 사용자 승인 있음. main 루트 GitHub Pages 자동배포 설정 확인, 주소 https://dave-nykim.github.io/DungeonMart/ . 이전 미커밋 직업/소환수/몬스터/영혼/이름/전직 아이콘 작업도 함께 배포 대상. API 키·인증 설정은 저장소에 없음.
- 제한/다음: 액트 보스는 단일 자세, 개별 연속 애니메이션 없음. 실제 폰 진행 속도/장비 기준 밸런스 피드백 필요. 새 PNG들로 자산 용량 증가, 초기로드 최적화는 후속. 오프라인 정산은 기존대로 레이드 중 없음.


## 2026-09-21 · 몬스터 동작/스킬 강화·영혼 회복소 부활

- 요청: 몬스터 움직임과 주기 스킬, 실제 난이도 상승. 용사 사망 시 영혼으로 회복소 이동 후 대기 부활.
- assets/monsters/motion.png 동일 16종 추가 자세 생성. nightmare-art.js 2자세 캐시/난이도 팔레트. 이동 시 자세 교대, 공격/시전 자세 및 좌우 방향·짧은 공격 이동, 스킬 직전 1초 발밑 예고. 8방향 연속 애니메이션은 아님.
- 일반 몬스터 스킬 간격 노멀9/나이트7.5/헬6초. 독/저주/흡수/전격 및 근접 강타·돌진 추가, 기존 화염 강화. 기본 공격 외 스킬 배율 1.3/1.65/2.0, 근접 계열 범위 제한. 부활/번식 기존 처리 유지. 스킬명·파동/투사체 표시. _moving/_facing/_attackPose/_casting 시각 상태는 저장에서 제외.
- 사망 위치 유지, dead 상태를 영혼 이동/회복소 대기로 사용. 공격/피격/자연 회복 없음, 소환수·버프 제거. 회복소(spring)까지 160속도로 지형을 통과하여 이동, 도착 후 12게임초 대기하고 최대HP 부활. 기존 장비 자동 부활 효과는 그대로. soulAtClinic 표시 상태 추가(없어도 위치로 재판정), 기존 dead 저장도 회복소로 이동. 회복소 위치 변경에 따라 목적지 재계산. 재화·장비·경험치 보존.
- 화면: 청록 영혼 실루엣·부유, 상태 문구 영혼 이동/부활 남은 초. 회복의 샘 표시명을 회복소로 변경, 도움말/README 갱신.
- 검증: 기존 전체 55개 통과, tests/soul-revival.test.mjs 2개 추가 통과(이동 중 타이머 정지·도착 대기·저장 왕복·부활, 난이도별 스킬 추가 피해/주기). 격리 390×844/360×640 몬스터 16종 자세 차이·3팔레트·알파·영혼 렌더 상태 복원·넘침/오류 없음. 문법·diff 검사 통과.
- 변경: engine.js, render.js, nightmare-art.js, app.js, world.js, README.md, monster motion 자산 및 tests/nightmare-browser-smoke.mjs, tests/soul-revival.test.mjs. 로컬만, 커밋/push 없음.
- 다음: 실기기 전투 난이도/회복소 왕복 시간 체감 보정. 12초는 게임 시간(기본2배속에서 실시간 약6초), 이동 시간이 별도 추가됨. 장기 밸런스는 추가 플레이 검증 필요.


## 2026-09-21 · 전 난이도 몬스터 원본 통일·3색 팔레트

- 요청: 노멀/나이트메어/헬은 동일 모양에 색상 차이, 현재 캐릭터와 어울리는 고품질 픽셀.
- 바바리안·소환수 이미지를 스타일 참조로 내장 생성한 16종 4×4 투명 시트 assets/monsters/sprites.png 추가. 기존 assets/nightmare 원본은 보존하되 런타임 사용하지 않음.
- src/nightmare-art.js 기존 로더 API 유지, 새 원본 사용 및 tier별 명도 기반 팔레트 변환/캐시. 노멀 원색, 나이트메어 보라/청색, 헬 적색/주황. 원본 밝기 단계·알파 보존, 저주 오버레이 유지.
- src/render.js 노멀 포함 전 난이도 일반/정예 몬스터 새 이미지 적용, 도감도 동일 기본 이미지. 보스·소환수는 기존 전용 이미지. 단일 자세/가벼운 상하 움직임이며 새 방향별 애니메이션은 없음. 능력치·드랍·저장 변경 없음.
- 검증: tests/nightmare-browser-smoke.mjs 격리 Whale 9223에서 390×844/360×640, 16종 투명 이미지·3색 차이·팔레트 간 알파 일치·넘침/오류 없음 통과. 문법·git diff --check 통과. 렌더 전용 변경으로 로직 테스트 추가 재실행 없음.
- 로컬만 적용, 커밋/push 없음. 다음: 실제 폰 다수 교전 가독성 확인.


## 2026-09-21 · 네크로맨서 소환수 픽셀 교체

- 요청: 기존 소환수 픽셀 개선. 내장 이미지 생성으로 녹색 눈·룬의 무장 해골기사, 녹색 핵의 철갑 골렘 2종×2자세 투명 시트 제작.
- assets/summons/sprites.png 원본 보존, src/summon-art.js 런타임 64px 추출/캐시. src/render.js에서 일반 몬스터 재사용 대신 전용 소환수 렌더. 해골 48px, 골렘 64px. 평소 불투명·소멸 전 2초만 페이드, 로드 실패 시 기존 이미지 사용. 전투·소환 시간·저장 구조 변경 없음.
- 한계: 2자세 교대 표시이며 공격 타이밍 연동/방향별 독립 애니메이션은 아님. 현재 엔진의 skeleton/golem 두 종류에 적용, 해골 마법사 등 스킬별 세부 외형 분리는 미구현.
- 검증: tests/summons-browser-smoke.mjs 격리 Whale 9223, 390×844·360×640에서 두 종/자세 차이·투명 배경·Canvas 상태 복원·넘침/오류 없음 통과. node 문법·git diff --check 통과. 게임 로직 변경 없어 npm test 추가 재실행 없음.
- 로컬 적용만, 커밋/push 없음. 기존 전직 아이콘 및 기타 미커밋 작업 보존. 다음: 실제 폰 사냥 중 크기·구분감 확인.


## 2026-09-19 · 용사별 색조·직업 이름·이름 변경

- 요청: 같은 직업도 색상을 조금씩 다르게, 직업 외형에 맞는 기본 이름, 이름 변경 기능.
- src/hero-identity.js: 직업별 10개 기본 이름(반복은 숫자 접미사), ID 기반 7색 개인 색조. 기존 수동 코스튬 색상 우선. 재접속/이름 변경에도 개인 색조 유지.
- 신규 영입 직업별 이름 적용. 기존 저장은 nameRevision 없는 공용 기본 이름만 직업별로 보정, 그 밖의 이름/사용자 변경 이름 보존. 장비·재화·능력치 보존. nameRevision=1/customName 저장.
- 용사 프로필 이름 변경 버튼→입력/취소/저장 모달. 공백 정리·한글/문자/숫자 1~12자 허용, HTML·기호/빈 값 거부. 저장 즉시 화면/로컬 저장 반영. 모바일 입력 48px·16px, 버튼 44px.
- 변경: src/hero-identity.js, engine.js, render.js, app.js, style.css, tests/hero-identity.test.mjs 및 기록 문서.
- 검증: 기존 npm test 54개 통과 후 새 identity 테스트 1개 별도 통과(이름 검증·구 저장 보정·사용자 이름 보존·색상 안정성). /tmp/identity-browser.mjs 격리 9223에서 390×844/360×640 이름 변경 저장·입력 타깃·넘침/런타임 오류 없음 확인. 문법·diff 검사 통과.
- 로컬 적용만, 커밋/push 없음. 다음: 실제 폰 색조 구분감 피드백. 개인 색조는 전체 이미지에 약한 색상을 입히는 방식이며 부위별 의상 교체는 아님.


## 2026-09-19 · 몬스터 참조 문구 제거·나이트메어 픽셀

- 요청: 도감 몬스터 구성 참조/디아블로 언급 제거, 나이트메어 몬스터 픽셀 개선.
- 구현: 도감 하단 외부 참조 링크·출처 설명 제거, README 참조 목록/언급 제거. 특정 작품에서 가져온 13종 표시 이름을 독자 이름으로 변경. 내부 몬스터 ID·전투 수치·저장은 그대로 보존.
- 내장 이미지 생성으로 독자 몬스터 16종 투명 4×4 시트 제작. assets/nightmare/sprites.png 원본, src/nightmare-art.js 캐시 로더. 나이트메어·헬 사각형 임시 이미지 제거, 종별 64px 픽셀·저주 색조·정예 크기·HP 표시 적용. 로드 실패 시 기존 일반 몬스터 픽셀 사용. 보스는 기존 이미지 유지.
- 한계: 종별 단일 자세와 렌더 상하 움직임이며 방향별 걷기/공격 애니메이션은 미구현. 헬도 같은 시트 사용. 도감 초상화는 노멀 이미지 유지.
- 검증: npm test 54개 통과, 문법·git diff --check 통과. tests/nightmare-browser-smoke.mjs 격리 Whale 9223에서 390×844·360×640 16종 고유 이미지/투명 배경/넘침·런타임 오류 검증. 실행 npm start 후 node tests/nightmare-browser-smoke.mjs. 실제 폰 검증 남음.
- 기존 미커밋 직업·보스 효과 작업 보존. 이번 변경 로컬 적용만, 커밋/push 없음. 다음: 실제 사냥 화면 피드백 및 필요 시 방향별 동작 제작.


## 2026-09-19 · 보스 스킬 효과 개선

- 요청: 보스 스킬 임팩트 개선. src/boss-effects.js Canvas 전용 효과 모듈 추가.
- 잿불 폭발: 110 반경 예고(쿨다운 마지막 1.2초), 확장 충격파·균열·튀는 잿불. 암흑 선회: 230 반경 예고, 실제 60도 부채꼴 표시·다층 광선·번개 잔상. 소환: 두 위치의 마법진과 상승 기둥. 예고는 기존 쿨다운 표시이며 공격 지연/피해/주기 변경 없음. 폭발/소환 시각 효과 수명 .9초, 입자 수 고정.
- 변경: src/boss-effects.js, src/render.js, src/engine.js. 저장 구조 변경 없음. 화면 전체 섬광/흔들림 없음.
- 검증: npm test 54개 통과. node 문법·git diff --check 통과. /tmp/boss-vfx-check.mjs 격리 브라우저 390×844·360×640에서 각 효과 시작/중간/끝 렌더 및 Canvas 상태 복원 확인. 실제 폰/장시간 성능 확인은 남음.
- 로컬 반영만, 커밋/push 없음. 다음: 사용자 보스전 체감 피드백에 따라 강도 조정.


## 2026-09-19 · 나머지 세 직업 픽셀 교체

- 요청: 바바리안·소서리스에 이어 나머지 직업도 제작. 내장 이미지 생성으로 네크로맨서(뼈 장식/녹색 마법), 아마존(청동 갑옷/활), 팔라딘(강철 갑옷/망치·방패) 제작.
- 각 assets/{necromancer,amazon,paladin}/sprites.png 원본 시트 보존. 4방향 × 대기/걷기/공격 자세, 64px 런타임 추출. 걷기는 2자세 교대, 공격은 단일 자세이며 8방향 독립 애니메이션 아님. 네크로맨서 왼쪽 열이 오른쪽으로 생성되어 렌더링 반전 보정.
- 공통 로더 src/hero-sheet.js로 소서리스도 통합, src/remaining-hero-art.js 추가. src/pixel-art.js 연결 및 기존 소서리스 중복 분기 제거. src/render.js 이름/HP 높이 적용. 장비 능력치/저장 변경 없음. 합성 이미지로 무기·갑옷 외형 고정(아마존 투창 전직도 활 이미지).
- 검증: tests/remaining-heroes-browser-smoke.mjs 격리 Whale 9223에서 세 직업 각각 390×844·360×640 로드/걷기 변화/투명 배경/가로 넘침·런타임 오류 없음. 초상화 스크린샷 확인. 문법 및 diff 검사 통과. 게임 로직 변경 없어 npm test 재실행 없음.
- 로컬 적용, 커밋/push 없음. 다음: 실제 폰 확인, 독립 대각선·연속 공격 프레임·전직별 무기 외형은 향후 작업.


## 2026-09-19 · 소서리스 직접 이미지 생성

- 요청: PixelLab MCP 대신 바바리안을 참고하여 소서리스 생성·적용.
- 구현: 내장 이미지 생성으로 은발/보라 로브/수정 지팡이 소서리스 투명 시트 제작. assets/sorceress/sprites.png 원본 보존. 4방향 × 대기/걷기 자세/시전 자세 3행, 런타임 Canvas에서 64px로 추출. 걷기는 대기/보폭 2자세 교대, 공격은 시전 자세. 대각선은 가까운 측면 사용(8방향 독립 생성·8프레임 애니메이션 아님).
- 연결: src/sorceress-art.js, src/pixel-art.js, src/render.js. 지도/초상화/색조와 이름·HP 위치 반영. 저장 구조·능력치 변화 없음. 합성 이미지라 장비별 무기/갑옷 외형은 고정.
- 검증: node tests/sorceress-browser-smoke.mjs 격리 Whale 9223에서 390×844·360×640 로드, 걷기 자세 변화, 투명 배경, 가로 넘침/런타임 오류 없음. 초상화 스크린샷 확인. node --check 및 git diff --check 통과. 로직 변화 없어 npm test 재실행 안 함.
- 로컬 적용 완료. 이번 소서리스 작업은 커밋/push 없음. 다음: 실제 휴대폰 및 추가 동작 품질 피드백.


## 2026-09-18 · PixelLab 바바리안 교체

- 요청: 남은 PixelLab 생성량으로 바바리안만 재제작·적용 후 main에 업로드.
- 구현: 64px 8방향 대기, 방향별 8프레임 걷기·도끼 공격. 공격 원본은 92px 여백 포함이며 중앙 여백만큼 발 기준점 보정. 실제 공격 횟수 변화로 0.8초 공격 애니메이션 재생. 지도 이동/타깃 방향 및 용사 초상화 연결, 이름/HP 위치 조정. 비동기 로드 실패 시 기존 픽셀 사용.
- 파일: assets/barbarian/ (원본 PNG 136개·PixelLab 메타데이터·manifest), src/barbarian-art.js, src/pixel-art.js, src/render.js, src/app.js, tests/barbarian-browser-smoke.mjs, 모바일 감사 문서.
- PixelLab character ID: f71a3b59-95d4-4141-aefd-6c438fe0b087. 무료 생성 18회 사용(기본 2+걷기 8+공격 8), 추가 구매 없음. 인증 정보는 저장소에 포함하지 않음. 직접 이미지 저장소 403은 인증된 MCP character download ZIP으로 해결.
- 검증: npm test 54개 통과, 문법 검사·git diff --check 통과. 격리 Whale 9223에서 equipment-browser-smoke 통과(390×844/360×640 터치·44px·넘침·오류). barbarian-browser-smoke에서 24개 방향/동작 로드, 걷기·공격 프레임 변화 및 두 모바일 화면/초상화 확인. 실행: npm start 후 격리 브라우저에서 node tests/barbarian-browser-smoke.mjs. 실제 휴대폰 검증은 미실시.
- 제한: 새 바바리안은 도끼/갑옷이 합쳐진 이미지라 착용 장비별 무기·갑옷 외형 교체는 없음. 능력치와 꾸미기 색조·장신구 광원은 유지. 다른 직업은 기존 픽셀 유지.
- 다음 작업: 배포 후 실제 폰 가독성 피드백. 다른 직업 제작은 이번 범위에 포함하지 않음.


## 2026-09-18 · PixelLab MCP 연결

- 요청: 용사 스프라이트 제작용 PixelLab MCP를 Codex에 연결. 사용자가 별도로 실행한 claude mcp add도 성공해 Claude 프로젝트 설정에 등록됨.
- Codex 글로벌 `~/.codex/config.toml`에 pixellab, streamable_http, https://api.pixellab.ai/mcp, Authorization 인증 등록. 파일 권한 0600. 키 값은 프로젝트·인계 문서에 기록하지 않음.
- 검증: 공식 MCP initialize와 tools/list 인증 성공, 도구 91개 확인(create_character, animate_character 등). codex mcp get 결과도 enabled와 Authorization 설정 존재를 비밀값 출력 없이 확인.
- 현재 대화 도구 목록에는 새 서버가 동적 로드되지 않음. Codex 재시작/새 세션에서 PixelLab 도구 로드 후 사용. 이번 작업은 연결만이며 이미지 생성·유료 생성 요청 없음.
- 게임 소스 변경 없음. 변경 문서 HANDOFF.md, docs/WORKLOG.md. 생성 작업은 별도 사용자 요청에 따라 진행. 기존 게임 변경은 미커밋/미배포 유지.


## 2026-09-18 · 난이도 재조정: 무장비 나이트메어 보스 통과 방지

- 요청: 전체적으로 너무 쉬움. 장비 없이 레벨만 올려도 나이트메어 1 보스가 쉽다는 피드백.
- 원인: 보스 HP/공격에 stage가 반영되지 않음. 기존 보스는 파티 전투력×14와 평균 HP×0.2에 낮은 난이도 배율만 적용해 장비 성장도 보스 능력치에 상쇄됨. 일반 몬스터도 노멀 10의 성장폭이 3.7배에 그침.
- 구현: `data.js` 일반 몬스터 기본 HP 70→90, 공격 9→14. 단계 배율 `1.25^(stage-1)`(10단계 약 7.45배). 난이도 몬스터 배율 노멀1/나이트메어20/헬400. 액트간 비율·보상 배율·성장/장비 수치는 유지.
- 보스 `bossStats(s)`: 난이도 기본 HP/공격 = 노멀 12,000/90, 나이트메어 240,000/900, 헬 4,800,000/6,000. HP에 단계 배율×max(1,용사 수/5), 공격에 `1.16^(stage-1)`. 레벨/등급/장비와 무관하므로 장비 강화가 실제 이득. 단계별 증가와 난이도 경계 격차 적용. 전투 패턴·4분 제한 유지.
- 저장: v3 유지, `combatRevision=1` 추가. 구 저장의 현존 일반/정예/부하/보스 최대HP·공격을 새 규칙으로 환산하고 현재 HP 비율 보존. 용사/장비/재화/난이도 해금/레이드 남은 시간 보존, 새 기준 중복 적용 방지. 미래 revision 거부.
- 검증: npm test 53개 전체 통과. 보스 단계/파티수/레벨 독립성, 구 전투 저장 환산·HP 비율·재로드, Lv.60 무장비 5명 나이트메어1 실패 회귀 테스트 추가. node --check·git diff --check 통과.
- 시뮬레이션(일반 등급·미전직 Lv.60, 교전 위치에서 시작): 나이트메어1 무장비 5명 240초 제한 실패·보스 HP 약64% 남음, 직업별 세트 6부위 착용 5명은 약126.7초 처치. 무장비 20명도 실패·HP 약29% 남음. 비교 스크립트 `/tmp/dungeon-balance.mjs`. 이는 특정 구성 검증이며 모든 직업/스킬/옵션 조합의 완전한 밸런스 보장은 아님.
- 변경 파일: src/data.js, src/engine.js, tests/engine.test.mjs, HANDOFF.md, docs/WORKLOG.md, README.md. UI 구조 변경 없음, 브라우저 테스트는 이번 재조정에서 재실행하지 않음.
- 로컬만 반영, 커밋/push/배포 없음. 다음 작업: 실제 진행 구간별 피드백으로 나이트메어 후반·헬 수치 보정. 제작 단계 자동화는 여전히 제안만, 미구현.


## 2026-09-18 · 창고 개수제·마을 통합·모바일 문구·메뉴 정리

- 요청: 창고는 크기 대신 아이템 1개당 1자리. 제작소를 마을 안 창고/제작소/꾸미기로 통합. 모바일 긴 설명 축약. 소식 탭 제거. 지도 아래 +/− 등 버튼 묶음 제거. 후속 질문: 제작 1/2/3단의 의미 설명(자동 단계 적용은 제안만 했으며 미구현).
- 구현: warehouseUsed는 warehouse.length. 제작·입고·장비 해제·희귀 장비 자리 확보 모두 1개 기준, UI 단위 개. 용량 80/100/120/144/168/192 유지. 저장 ID 목록·장비 보존, 저장 구조 변경 없이 기존 저장도 새 계산 적용. 용사 장착 격자 크기는 기존 규칙 유지.
- UI: 하단 맵/용사/사냥터/마을/도감/설정 6개. 마을 상단 창고/제작소/꾸미기 탭(44px 이상), 제작소 DOM을 마을 내부로 이동. 지도 대장간도 마을 제작소로 진입. 꾸미기에서 다른 탭으로 이동하면 편집 종료. 창고의 null 용사 참조 버그도 수정하여 장비 상세·분해 가능. 시설 개선은 제작소에서 제공.
- 모바일: 반복 설명·제작 완료 알림·버튼 문구 축약, 제작 입력칸 1열, 긴 장비명/상세 줄바꿈과 버튼 고정, 마을 탭 상단 고정. 소식 화면 DOM/렌더 제거, 내부 게임 로그 저장은 보존. 지도 확대/축소/배율/넓히기 UI와 핸들러 제거, 핀치·휠·지도 이동 유지.
- 변경 파일: src/engine.js, src/app.js, src/gear-ui.js, src/equipment-codex.js, index.html, style.css, tests/items.test.mjs, tests/browser-smoke.mjs, tests/equipment-browser-smoke.mjs, README.md 및 인계/작업/모바일 문서. 앞선 미커밋 작업 그대로 유지.
- 검증: 창고 크기 독립·80개 경계·제작 실패 시 재료 보존·장비 해제·세트 입고 시 1개만 분해·저장 왕복·100개 확장 테스트 추가. `npm test` 50개 전체 통과. 이후 UI 변경은 문법 검사·git diff --check 통과. 최종 `node tests/equipment-browser-smoke.mjs` 격리 Whale 9223에서 390×844·360×640 통과: 마을 3탭, 장비 상세, 개수 표기, 메뉴 제거, 44px 버튼/입력칸, 가로 넘침 없음, 실제 터치 핀치 확대, 제작·도감, 런타임 오류 없음. 화면 `/tmp/dungeonmart-review/mobile-craft-360.png` 등.
- 실행: npm start. 브라우저 테스트는 격리 테스트 저장을 덮어씀. 전체 browser-smoke는 기존 용사 추적 assertion 실패 이력이 있으며 이번에는 집중 검증만 실행. 실제 폰 확인 미실시.
- 로컬 수정만. 커밋/push/배포 미실시. 남은 논의: 제작 단계 자동 적용 여부는 사용자 결정 전이며 현행 1/2/3단 선택 유지.


## 2026-09-18 · 희귀 장비 전용 드롭·랜덤 제작·장비 도감

- 요청: 필드 장비 드롭은 세트·유니크만, 확률 대폭 하향. 일반·매직·레어는 제작 시 등급과 옵션 무작위. 후속 요청: 세트·유니크 도감 추가.
- 드롭 구현: `items.js DROP_TABLE/rollDropGrade`는 처치당 기본 확률 일반 몬스터 세트 0.002%+유니크 0.001%, 정예 0.08%+0.07%, 보스 1.5%+1%. 몬스터당 1회 판정, 실패 시 장비 없음. 장비 발견 옵션은 기존 최대 1.9배 유지. 첫 보스 유니크 확정·3회 보장·소프트 천장 제거(구 저장 pity 필드는 호환용으로 남지만 미사용). 해당 ilvl에 생성 가능한 세트/유니크가 없으면 일반 장비로 대체하지 않고 드롭 없음. 보석·각인석·재료 획득은 유지.
- 제작 구현: `craft(s, baseKey, tier=1, rng=Math.random)`, `craftCost(tier)`로 API 변경. 등급 선택 제거, 일반 60%·매직 30%·레어 10%. 일반은 기존 기본 성능/품질/홈 규칙, 매직·레어는 무작위 접사와 수치. 부적도 제작 등급 확률을 따른다. 비용은 기존 일반 제작 기준 철 8/20/40(1/2/3단), 제작대 제한 유지. 기존 보유·필드 장비 삭제 없음.
- 도감 구현: `src/equipment-codex.js` 신규. 하단 도감 → 몬스터/세트/유니크 3탭. 세트 5종·30부위의 구성·옵션·2~6부위 효과, 유니크 24종의 옵션·직업 제한·장비 레벨·획득 여부. 미획득도 공개. HTML details 터치로 펼침, 자동 갱신 후 펼침 유지. 수치는 1단 기준값이며 실제 드롭은 변동됨을 안내.
- 저장: v3 유지, `codex.setPieces`(세트ID:부위 → 1), 아이템 `codexRecorded` 추가. 입고/수동 획득/만료 자동 입고 시 기록, 분해 후에도 유지. 기존 창고·장착 장비에서 부위 기록 복원, 기존 유니크 기록 유지, 미획득 빛기둥 제외. 예전에 분해한 세트의 개별 부위는 구 저장에 정보가 없어 복원 불가(기존 codex.sets 최고 동시 착용 수는 그대로 보존).
- 변경 파일: src/items.js, src/engine.js, src/gear-ui.js, src/app.js, src/equipment-codex.js(신규), style.css, tests/items.test.mjs, tests/engine.test.mjs, tests/equipment-browser-smoke.mjs(신규), README.md, HANDOFF.md, docs/WORKLOG.md, docs/MOBILE_UI.md. 앞선 난이도·배속 수정도 같은 작업 트리에 미커밋 상태로 유지.
- 검증: `npm test` 49개 통과, 문법 검사·git diff --check 통과. 격리 Whale DevTools 9223에서 `node tests/equipment-browser-smoke.mjs` 통과: 390×844·360×640 실제 터치 입력으로 제작, 등급 선택 없음/확률 안내, 철 8 소모·장비 생성, 도감 5/24종·펼침 유지·획득 마이그레이션·44px 타깃·가로 넘침 없음·런타임 오류 없음. `/tmp/dungeonmart-review/mobile-craft-*.png`, `mobile-sets-*.png`, `mobile-uniques-*.png`.
- 실행: `npm start`. 신규 브라우저 테스트도 격리 9223 테스트 프로필 전용이며 테스트 저장을 덮어쓴다. 개인 브라우저 사용 금지. 전체 browser-smoke는 이번에 재실행하지 않았으며 이전 작업에서 용사 추적 assertion 실패 기록이 있다.
- 남은 일: 실제 폰 확인·드롭/경제 밸런스 플레이 피드백. 로컬 수정만 완료, 커밋·push·배포 미실시.


## 2026-09-18 · 단계별 보스 해금과 기본 2배속

- 요청: 노멀 1~10 자유 선택 제거, 기존 보스 처치 해금 규칙을 단계마다 적용. 모바일 보스 HP와 겹치는 1×/2× UI 제거, 기본 2배속.
- 구현: 노멀 1 보스 → 노멀 2 … 노멀 10 보스 → 나이트메어 1, 헬 10까지 동일 규칙. 이미 해금한 단계 재방문 가능, 변경 후 게임 시간 5분 잠금과 레이드 중 변경 금지 유지. `difficulty.unlockedStage` 추가. 구 저장은 현재 단계와 이미 해금된 하위 난이도를 보존하며, 아직 방문하지 않은 상위 단계는 순차 해금한다.
- 배속: 새 게임/구 저장 모두 speed=2로 변환, 프레임 시뮬레이션 2배 고정. 배속 DOM·렌더·클릭 처리 제거. 난이도 단계 잠금 및 안내를 모바일에서도 표시, 버튼 44px 이상.
- 변경 파일: src/engine.js, src/app.js, index.html, style.css, tests/engine.test.mjs, tests/browser-smoke.mjs, HANDOFF.md, docs/WORKLOG.md, docs/MOBILE_UI.md.
- 검증: `npm test` 47개 통과. 최종 해금 경계 조정 후 관련 테스트 3개 재통과. `node --check` 통과. 격리 Whale 9223에서 390×844·360×640 터치로 사냥터 진입, 단계 잠금·44px 타깃·가로 넘침 없음·보스 HP 표시·배속 UI 없음·저장 speed=2 확인. 스크린샷 `/tmp/dungeonmart-review/mobile-difficulty-390.png`, `mobile-difficulty-360.png`. 집중 검증 스크립트 `/tmp/dungeonmart-mobile-check.mjs`.
- 제한: 전체 `node tests/browser-smoke.mjs`는 기존 용사 추적 이동 assertion(61행)에서 실패하여 전체 통과 아님. 이번 변경 범위는 별도 모바일 검증 통과. 실제 폰 검증 미실시.
- 로컬 수정만, 커밋·push·배포하지 않음. 실행 `npm start`. 다음 작업: 전체 스모크의 용사 추적 검증 안정성 확인, 사용자 배포 지시 시 배포 후 실기기 확인.


## 2026-09-11 · GitHub main 업로드 요청

- 요청: 현재 버전을 GitHub 원격 main에 업로드. 사용자 명시 승인으로 기존 로컬 커밋을 push한다.
- 현재 게임 구현은 `dfe2de8`까지 main에 병합되어 있으며, 이번 작업의 변경 파일은 HANDOFF.md와 docs/WORKLOG.md뿐이다.
- 검증: `npm test` 46개 전체 통과. 브라우저 스모크는 이번 업로드 작업에서 재실행하지 않았다.
- 실행: `npm start`. 모바일 접속 주소: https://dave-nykim.github.io/DungeonMart/.
- 다음 작업: push 후 원격 main과 로컬 HEAD 일치 확인. GitHub Pages 반영은 별도 배포 처리 시간에 따라 지연될 수 있다. 기존 기능의 남은 범위는 아래 기록 참고.

마지막 갱신: 2026-09-11 (Claude 인계 후 7차 작업: 보스 확대·8방향·암흑 선회 스킬. 6차: 보스 스프라이트 PixelLab 이미지 교체. 5차: 난이도 사다리. 3차 작업: 보스 레이드, 몬스터 분산 스폰, 지옥 지형 정리, 클릭형 전직 트리). 2차 작업: 용사 관리 UI 정리, 사냥터 드래그앤드롭 보드, 마을 대기 상태, 픽셀 밀도 2배(1 픽셀 = 1 월드 단위) 구현. 이전 작업: 연결형 자연 지형, 항구 입항, 마을 편집, 용사 팝업 내부 관리, 스킬 아이콘/미리보기. **장비 시스템 v2는 설계만 완료(docs/EQUIPMENT_DESIGN.md), 미구현.** **장비 시스템 v2 구현(feature/equipment-v2 브랜치): 격자 장비창·드롭·빛기둥·홈/각인석/진언·세트/유니크·조합·저장 v3.**

## 사용자 방향과 최신 요청

- **2026-09-11 최우선 방향: 핸드폰으로 하는 게임.** 모든 UI를 터치 친화적으로 만든다. 규칙·감사 목록은 `docs/MOBILE_UI.md`. 데스크톱은 보조. 새 UI는 세로 폰 뷰포트에서 먼저 만들고 넓힌다.
- 배포는 GitHub Pages(`https://dave-nykim.github.io/DungeonMart/`, main 브랜치). **사용자가 배포하라고 할 때만** 커밋·push한다. 그 전까지는 로컬 수정만.
- 플레이어는 던전 편의점 주인. 용사 최대 20명은 자동 사냥·귀환·회복. 주인이 장비·전직·스킬·마을을 관리한다.
- 맵은 항상 표시. 관리 메뉴는 하단, 용사 상세는 큰 팝업. 운영금/재료/저장/배속은 맵 안 HUD.
- 최신 요청: 원형 섬 4개를 없애고 배경이 연결된 자연스러운 대륙으로 변경. 항구는 마을 서쪽, 액트 IV는 동쪽, 액트 I/II/III는 대략 남서/남부/남동. **사용자의 후속 정정: 이 배치를 격자로 맞추지 말 것. 크기와 경계가 달라도 됨. 액트 I이 북서쪽으로 올라가도 됨. 북쪽 전체를 빈 줄로 남기지 말고 일부 확장 공간만 자연스럽게 확보.**
- 마을 시설 위치를 옮기고 장식하는 기능. 용사 팝업 안에서 장비/전직/스킬/사냥 배정/기록을 보고 조작. 용사 따라가기. 용사 개성과 스킬 아이콘. 미전직 스킬도 효과 미리보기.
- AGENTS.md 준수: 추가 패키지 없는 로컬 웹 게임, 저장 보존/마이그레이션, 완료 때 이 파일과 docs/WORKLOG.md 갱신. 이미지 외부 복제 없음. 코드로 직접 그린 픽셀 아트 및 SVG 아이콘.

## 2026-09-11 · Claude 7차 작업 (보스 확대·8방향·암흑 선회, feature/boss-spin)

요청: 보스가 작다 → 확대. 8방향 시트를 써서 8방향으로 돌며 암흑 마법을 쓰는 보스 스킬. 보스 HP HUD와 "소환중" 버튼 겹침 → 레이드 중 버튼 숨김.

- `assets/boss.png`는 이제 1536×192 가로 스트립(방향 8칸: S, SW, W, NW, N, NE, E, SE). 원본 PixelLab 시트의 크림 배경은 가장자리 flood fill + 13px 초과 갇힌 영역 제거로 지웠다(정면 셀 기준 정상 하이라이트는 12px 이하). 256→192 면적 평균 축소. 기본 줌 1.25에서 화면 240px, 용사(40px)의 6배.
- `pixel-art.js bossSprite(frame, facing)`: 이미지 높이로 셀을 나눠 방향별 캐시. 이미지 없으면 기존 코드 스프라이트(방향 무시).
- `engine.js`: `e.facing`(0~7)은 매 틱 목표 용사 방향(`facingTo`). `SPIN` 상수: 소환 9초 후 첫 선회, 쿨 18초, 0.3초마다 한 방향씩 8칸, 각 방향 사거리 230·60° 원뿔 안 사냥 중 용사에게 공격력 55% 피해 + `buffs.weaken` 3초. 선회 중 이동·근접·잿불·소환 없음. 이펙트 `dark`(보라 광선). 저장: `spinCd`, `spin{started,step}`, `facing` 복원 검증(잘못되면 초기화).
- `render.js`: 방향별 스프라이트, HP 바 폭 = 스프라이트 폭 절반, 선회 중 룬 링.
- `app.js renderRaid`: `state.raid`면 `#summon-boss` hidden.
- 검증: `npm test` 통과(선회 테스트 추가). 브라우저 미확인(Chromium 없음). 확인 방법: `PORT=4174 npm start` 후 보스 소환, 9초 뒤 선회.
- 조정 포인트: `SPIN` 상수, 보스 크기는 PNG 교체만으로 변경 가능(정사각 셀 8칸 스트립 유지).

## 2026-09-11 · Claude 6차 작업 (보스 스프라이트 PixelLab 이미지 교체, feature/boss-sprite)

요청: PixelLab으로 만든 보스 그림을 게임에 적용. **사용자 방향 변경: 외부 픽셀 아트 도구(PixelLab) 생성 이미지 사용을 허용한다.** 용사(24×32)도 나중에 더 큰 크기로 다시 뽑을 계획이며, 그때 몬스터·건물도 같은 비율로 교체해야 한다.

- `assets/boss.png`(125×123): 원본 시트(256×256 셀 8방향, 사용자 다운로드 폴더의 `pixellab-cute-wizard-1789113682677.png`) 정면 셀을 2×2 평균으로 축소. 다른 방향 셀은 배경이 박혀 있어 미사용. 발은 이미지 하단 가장자리, 기준점은 `h-2`.
- `pixel-art.js`: `bossSprite`가 `new URL('../assets/boss.png', import.meta.url)`로 이미지를 로드. 로드 후 캐시를 비워 이미지 프레임으로 전환하고, 실패 시 기존 코드 스프라이트(64×72) 유지. 캔버스에 `anchorY`(발 y) 부여.
- `render.js drawMonster`: 보스 그림자 폭, 불씨 궤도, HP 바(스프라이트 위 12px), 이름 배지 위치를 `sprite.width/height/anchorY`로 계산. 월드 레이어는 1 픽셀 = 1 월드 단위이므로 기본 줌 1.25에서 보스는 화면 약 156×154px, 용사는 30×40px.
- `server.mjs`: `.png` MIME 추가.
- 검증: `npm test` 40개 통과. 이 기기에 Chromium이 없어 브라우저 화면·browser-smoke 미확인. 확인 방법: 워크트리에서 `PORT=4174 npm start` 후 보스 소환.
- 남은 일: 화면에서 보스 크기·HP 바 위치 확인, 도감(bestiary)에 보스 초상 추가 여부, 용사·몬스터 교체 시 기준 크기 확정, 보스 걷기/공격 프레임(현재 호흡 1px만).

## 2026-09-11 · Claude 6차 작업 (지도 UI 정리 · 배속 · 보스 쿨타임 초기화 · 용사 선택 제거 · 세트 보호)

요청 원문 요약: 마을/사냥터 바로가기는 아이콘만 남겨 미니맵 위로(겹침 제거), 현재 위치 표시도 미니맵 위로. 배속 저장·2배까지만·멈춤 제거. 상단 헌터 추적·마을 편집·전체 화면 아이콘 제거(용사 추적은 용사 팝업에서). 아이콘은 1/4 크기, 보스 소환은 좌상단으로. 보스 아이콘에 쿨타임 표시, 클릭 시 남은 시간 계산해 돈 내고 초기화. 용사가 항상 선택돼 있는 상태 제거(용사 화면에서 고를 때만 상호작용, 소식 등 다른 곳의 용사 진입점 삭제). 세트 아이템이 창고로 안 들어오는 문제.

- **지도 코너**(`.map-corner`, 우하단): 현재 위치 칩(`#map-location`) → 아이콘 바로가기(마을 `shop` 아이콘, ACT I~IV는 로마숫자, 잠김은 자물쇠; 22px, 터치 24px) → 미니맵. `.map-topline`에는 DAY·난이도 라벨만 남음. 헌터 추적·마을 편집·전체 지도 버튼 삭제(전체 지도는 키 `0`, 추적은 용사 팝업 "이 용사 따라가기", 마을 편집은 하단 마을 메뉴).
- **보스 소환**(`.boss-corner`, 좌상단): 해골 아이콘 + 텍스트("보스 소환" / 쿨타임 mm:ss / "전투 중"). 쿨타임 중 클릭 → 모달 "봉인문이 닫혀 있습니다": 남은 분 × 100 G(`RAID_RESET_PER_MINUTE`, `raidResetCost`)로 즉시 초기화(`resetRaidCooldown`). `renderRaid`가 0.65초마다 라벨 갱신.
- **배속**: `state.speed`(1|2)로 저장·복원, 버튼 1×/2×만. 일시정지 버튼 삭제. 디버그/테스트용으로 Alt+P 토글만 남김(스모크 테스트 `pauseToggle`).
- **용사 선택 제거**: 지도에서 선택 용사 강조 괄호·항상 표시 이름·HP 바 특례 삭제(이름은 줌 85% 이상에서 모두 표시). 지도 클릭으로 용사 팝업 열기 삭제. 사냥터 보드 칩은 드래그 전용(클릭 시 팝업 없음). 관리 패널 헤더의 "선택 용사 · 정보" 버튼 삭제. 마을 창고(town 모드)는 `hero:()=>null`로 보기/분해 전용 — 장착은 용사 팝업 장비 탭에서만. 내부 `selected` 변수는 팝업이 보여줄 용사 용도로만 남음.
- **세트/유니크 보호**(engine `storeItem`): `PROTECTED_GRADES`는 절대 자동 분해되지 않음. 창고가 부족하면 `makeRoom`이 노멀→매직→레어 순으로 싼 장비를 분해해 자리를 만들고, 그래도 안 되면 `pickupFieldDrop`이 실패하며 빛기둥이 지도에 남는다. 5분 만료 시에도 못 넣으면 5분 연장 + 경고 로그. 원인: 창고 80칸이 귀환 잡템으로 차면 세트도 "창고가 가득 차 자동 분해"되던 기존 동작. (다음 작업에서 "가득 차면 아예 줍지 않음"으로 바뀔 예정.)
- 검증: `npm test` 45개. 스모크: 멈춤 버튼 대신 Alt+P, 전체 지도 대신 키 0, 칩 클릭 시 팝업 없음 확인, 모바일 40px 검사 대상에서 코너 아이콘 제외(사용자 요청으로 작게 만듦)하고 `.boss-corner`는 44px 유지.

## 2026-09-11 · Claude 5차 작업 (난이도 사다리: 노멀/나이트메어/헬 × 10단계)

요청: 난이도 3종 × 10단계, 단계마다 몬스터 강화, 노멀 10 ≪ 나이트메어 1(큰 갭). 고난이도 몬스터 외형은 우선 네모 박스(디자인은 나중에). 난이도는 바꾸면 5분 잠금, 그 난이도의 보스를 잡아야 다음 난이도 해금. 난이도 안에서 액트 I이 가장 약하고 액트 IV가 조금 더 셈(너무 크지 않게).

- 데이터(`data.js`): `DIFFICULTIES` 노멀 mult 1 / 나이트메어 8 / 헬 60(보상 ×1/3/8, 보스 ×1/1.6/2.6, 색). `STAGES=10`, `stageMult(stage)=1+(stage-1)×0.3`(10단계 3.7배). `ACT_FACTORS` 체력 [1,1.3,1.7,2.2], 공격 [1,1.25,1.55,1.9], `BASE_MONSTER` hp 70 / atk 9. **ZONES의 hp/atk 필드는 더 이상 쓰지 않음**(xp/gold는 액트별 차등 유지).
- 엔진: `s.difficulty={tier,stage,unlocked,lockedUntil}`. `zoneStats(s,zone)`이 hp/atk/xp/gold/mat 배율을 계산하고 `spawnEnemy`·`kill`·보스 보상이 이를 사용. 몬스터에 `tier` 저장. `setDifficulty(s,tier,stage)`: 해금 확인 → 같은 값/레이드 중/잠금(`DIFFICULTY_LOCK`=300초 게임 시간) 거부 → 적용 후 보스·부하 외 몬스터/시체 제거, 잠금 시작. 보스 처치 시 현재 tier가 unlocked와 같으면 unlocked+1(최대 헬), 로그 `boss`.
- 실제 배율 예: 노멀 1단계 ACT I 좀비 hp 105 → 노멀 10단계 389 → 나이트메어 1단계 840 → 나이트메어 10단계 3,108 → 헬 1단계 6,300 → 헬 10단계 23,310. 액트 IV는 같은 단계의 2.2배. 예전 액트별 고정 hp(70/270/750/1700)는 사라졌으므로 노멀 1단계 ACT IV가 이전보다 훨씬 약하다(단계가 진행 축). 액트 해금 레벨(8/20/35)은 그대로.
- 렌더: `e.tier>0`이면 `drawMonster`가 난이도 색 네모 박스(눈 2개, 위에 몬스터 고유색 띠, 정예는 큼)를 그린다. 나중에 실제 스프라이트로 교체할 자리.
- UI: 사냥터 화면 상단 `.difficulty-bar`(난이도 탭 3개, 잠긴 난이도는 자물쇠·비활성, 단계 1~10 버튼은 클릭 즉시 적용, 잠금 남은 시간 `#difficulty-lock`은 0.65초마다 갱신). 맵 우상단 DAY 라벨에 "노멀 3단계" 표시. 저장 필드 추가만(version 2), 구 저장은 노멀 1단계·미해금으로 시작. tier>unlocked 등 비정상 값은 복원 거부.
- 검증: `npm test` 41개(난이도 테스트 추가). 브라우저: 단계 즉시 적용/잠금/나이트메어 비활성, 나이트메어 3단계 fixture에서 박스 몬스터·hp 1,344~1,523 확인. 스크린샷 `difficulty-nightmare.png`, `box-monsters.png`.
- **테스트 실행 주의**: 다른 세션(워크트리 `DungeonMart-equip`)이 4173/9223을 쓰고 있을 수 있다. `tests/browser-smoke.mjs`는 `BASE`, `DEVTOOLS` 환경변수로 포트를 바꿀 수 있다(예: `PORT=4174 npm start`, Whale `--remote-debugging-port=9224`, `BASE=http://127.0.0.1:4174 DEVTOOLS=http://127.0.0.1:9224 node tests/browser-smoke.mjs`). 다른 세션의 서버/브라우저를 pkill로 죽이지 말 것.
- 남은 것: 고난이도 몬스터 실제 디자인, 장비 드랍 ilvl에 난이도 반영(미적용), 단계별 해금 조건 없음(난이도 내 자유 선택), 헬 이후 단계 없음.

## 2026-09-11 · Claude 5차 작업 (모바일 터치 전환 1차) — 로컬 수정, 미커밋

요청: "무조건 터치에 맞게. 핸드폰으로 하는 게임. 모든 UI 터치 친화적." 규칙 문서 `docs/MOBILE_UI.md` 작성 후 1차 코드 반영. **배포·커밋은 사용자가 지시할 때만.**

- 맵: 몬스터 탭 → 툴팁 2.6초 고정(`showTip/hideTip/enemyAt`, app.js). 호버 툴팁은 마우스 보조로 유지. 빈 곳 탭은 툴팁 닫기.
- 마을 편집 터치: `townPick` — 첫 탭 고스트 미리보기(`townGhost(point,true)`), 같은 자리(28 단위 이내) 재탭 또는 `#town-confirm` '여기에 배치'로 확정. 마우스/지우기는 즉시.
- 보스 소환: 확인 시트(`confirm-summon`, 비용·현재 운영금 표시). `index.html`의 title 제거. 스모크 테스트도 확인 탭 추가.
- 분해: `gear-ui.js` 인라인 확인(`ui.confirmSalvage`, `salvage` → `salvage-confirm`/`salvage-cancel`). 반환 재료를 보이게 표시.
- title 의존 제거: 스킬 강화 버튼 `+` 안에 `nP`, 무게 페널티 문장(`.weight-note`), 배치 버튼 "자리 없음", 빛기둥 배지 "눌러서 이동".
- CSS: style.css 끝 "모바일 터치 우선" 블록. `touch-action:manipulation`, `:active` 피드백, `@media(pointer:coarse)` 탭 타깃 44px 계열(세부는 MOBILE_UI.md 8절), 폰에서 `#modal` 전체 폭·90dvh, 맵 바로가기 2줄.
- 문구: 클릭/휠/드래그 → 누르기/핀치/끌기.
- 마을 메뉴 재구성: `townMode`('warehouse'|'edit'). 하단 마을 = 창고 기본(`gear-ui.js mountTown/unmountTown`, 클릭·change 핸들러를 `onClick/onChange`로 분리해 두 호스트에 바인딩). '마을 편집' 버튼/맵 바로가기가 `town-mode` edit, '편집 완료'가 warehouse. 나가면 `unmountTown`.
- 변경 파일: src/app.js, src/gear-ui.js, index.html, style.css, tests/browser-smoke.mjs, docs/MOBILE_UI.md, AGENTS.md, README.md, docs/DESIGN.md, HANDOFF.md, docs/WORKLOG.md.
- 검증: `npm test` 41개 통과, `node --check` 통과. **브라우저 검증은 미완(로컬 Chromium 없음).** 다음 담당자는 격리 브라우저에서 `tests/browser-smoke.mjs` 실행과 폰 실기기 확인 필요.
- 주의: 같은 시각 다른 세션이 난이도 기능(`difficulty`, data.js/engine.js/render.js/app.js)을 같은 체크아웃에서 작업 중. 커밋 시 두 작업을 분리해서 올릴 것.

## 2026-09-11 · Claude 4차 작업 (오프라인 재료 보상)

요청: 게임을 꺼 둬도 재료를 알아서 모아 오는 오프라인 보상. 사용자가 제안한 "실제 시뮬레이션 대신 측정 수익률로 정산" 방식을 승인.

- 엔진 `s.yield={rate,acc,since}`: `deposit`(귀환 입고)마다 `acc`에 재료를 더하고, `tick`의 `rollYield`가 게임 시간 120초마다 분당 획득량을 `rate = rate*.6 + 측정치*.4`로 갱신. 마을 대기·레이드 등 실제 입고가 없으면 자연 감소. 새 게임은 rate 0이라 2분 이상 플레이해야 정산이 시작된다.
- `settleOffline(s, since, now)`: 경과 초를 `OFFLINE.capSeconds`(8시간)로 캡, 60초 미만·레이드 중·미측정(모두 0)이면 null. 지급 = floor(rate × 분 × 0.6). 재료(철·마력석·영혼)만, 골드/경험치 없음. 로그 타입 `loot`. 반환 `{seconds,gained,capped}`.
- 앱: 로드 시 `state.savedAt`(serialize가 기록) 기준으로 정산 후 즉시 저장 → 모달 "오프라인 보상"(재료별 +, 분당 수익률, 효율). 탭을 숨겼다가 돌아오면 `hiddenAt` 기준으로 같은 정산(숨긴 동안 시뮬레이션이 멈추는 것을 보정). 안내 모달 문구 갱신.
- 저장: `yield` 필드 추가(version 2 유지). 없거나 잘못되면 fresh로 초기화. `savedAt`은 숫자가 아니면 제거.
- 검증: `npm test` 40개 통과(측정→정산→캡→1분 미만→레이드 중→구 저장 복원). 격리 브라우저에서 2시간 5분 전 저장 fixture로 모달·재료 증가·재로드 시 중복 없음 확인(`/tmp/dungeonmart-review/offline-modal.png`).
- 조정 포인트: `OFFLINE.efficiency`(0.6), `capSeconds`, `minSeconds`, `window`. 남은 아이디어: 골드/경험치 확장, 오프라인 중 장비 드랍은 미지원.

## 2026-09-11 · Claude 3차 작업 (보스 소환 · 몬스터 분산 · 지옥 정리 · 전직 트리)

요청 원문 요약: (1) "보스 소환"을 누르면 ACT IV에 악마가 나오고 전체 용사가 이동해 다같이 잡기. 디자인은 디아블로를 따라하지 말고 직접 픽셀로. (2) 몬스터가 액트 한곳에만 몰려 나오는 문제. (3) 불타는 지옥에 픽셀이 뭉쳐 보이는 생성물 제거. (4) 전직 UI를 디아 2 스킬 트리 느낌의 트리 모양으로, 그냥 클릭하면 전직되게.

### 보스 레이드 (엔진 `summonBoss` / `s.raid`)
- 맵 바로가기의 **보스 소환** 버튼(`data-action="summon-boss"`). 운영금 `RAID_COST`=500 G, 진행 중이면 비활성("보스 전투 중"). 소환 시 카메라가 보스로 이동.
- 보스 `MONSTERS.ashlord` "재의 군주 바라칸"(trait `boss`, 도감 목록에는 아직 없음). `BOSS_LAIR`(3104,1120) 봉인문 앞에 출현. HP = max(3000, 파티 총 전투력×14), 공격력 = 평균 용사 HP×0.2(방어력 적용 전). 7초마다 반경 110 잿불 폭발(0.7×), 15초마다 부하 2마리(최대 6, `minion:true`), HP 30% 미만 공속 증가. 공포 무시.
- `s.raid={bossId,started,ends}`, 제한 `RAID_DURATION`=240초. 레이드 중 모든 용사의 유효 사냥터는 3(`zoneOf`), 마을 대기도 일시 해제(`restingOf`), 전리품 12회 귀환 조건 무시(HP 28% 귀환은 유지). 출발 목표는 보스 위치. 몬스터 감지 범위는 보스 260.
- 처치 시 `endRaid('win')`: 기여 용사 전원 XP 290×20, 골드 65×25, 영혼 결정 3, 처치 수 +1, 운영금 현상금 2,000 G, `bossKills`+1, 로그 타입 `boss`. 시간 초과/보스 소실 시 보상 없이 종료. 종료 시 보스·부하 제거, 용사는 원래 배정(대기 포함)으로 복귀.
- 저장: `raid`, `bossKills` 필드 추가(version 2 유지). 복원 시 보스 없는 raid는 null로, raid 없으면 보스/부하 제거. 로그 타입 화이트리스트에 `boss` 추가.
- 렌더: `pixel-art.js bossSprite`(64×72, 2프레임, 원본 디자인: 잿빛 피부·불씨 균열·뼈 뿔·찢긴 날개). 원본 해상도로 그려 용사 대비 2.25배. 주변 불씨 파티클, 큰 HP 바, 이름 배지, 미니맵 깜빡이는 마커. 맵 상단 `#boss-hud`(이름·HP·교전 인원·남은 시간)는 `renderRaid()`가 0.65초마다 갱신.
- 검증값: 레벨 40 파티 5명(스킬 미투자)이 소환 후 74초에 처치(전투 약 60초). 레벨 1 파티는 HP 3,000 최소치라 오래 걸리고 자주 귀환한다.

### 몬스터 분산 스폰
- `world.js CAMPS`가 지역 타원 안의 13개 후보점(중앙·8방·중간)을 지역 판정/도로/지형 차단/POI 170 거리로 걸러 액트당 7~10곳(반경 120). 스폰 반경은 캠프 반경의 15~100%. 이전에는 중앙 근처 3곳 반경 55.
- 용사는 가장 가까운 몬스터를 찾아 액트 전체를 돌아다니므로 사냥 동선이 넓어졌다. 정원(7~18)은 그대로라 밀도는 낮아짐.

### 불타는 지옥 정리
- 무작위 용암 줄무늬(`i%7` stroke) 제거, 고사목/바위 밀도 약 40%로 감소, 지옥 타일 팔레트 저대비화, 인접 지형 디더링 폭 축소(56). 봉인문 앞이 읽기 쉬워졌다. 대형 용암 균열 13개(충돌)는 유지.
- 이후 대형 용암 균열도 제거(2026-09-11). 획 13개가 x 3px 간격으로 겹쳐 세로 막대처럼 뭉쳐 보였음. `world-art.js`의 stroke 루프와 `world.js terrainBlocked`의 균열 충돌 사각형을 함께 삭제. 액트 IV 지형 충돌은 이제 미개척 산악(RESERVE)뿐.

### 전직 트리 (클릭 즉시 전직)
- `renderSkills`: `.d2-tree`(ul/li CSS 커넥터, 금색 선) 1차 → 2차 2개 → 3차 4개 노드. 노드 아이콘은 계열 첫 스킬 아이콘. 상태 `chosen/eligible/waiting/other`, 선택 노드 `focus`.
- `data-action="tree-node"`: 조건 충족(레벨·상위 계열)이면 확인창 없이 `promote` 즉시 실행(재료 부족은 토스트). 아니면 노드를 선택해 아래 `.tree-detail`에 계열 설명·전직 비용·스킬 목록(미리보기 버튼) 표시하고 첫 스킬을 미리보기로 재생.
- 기존 `promote`/`confirm-promote` 핸들러는 남아 있지만 UI에서는 더 이상 쓰지 않는다. 배운 스킬 강화 섹션은 그대로.

### 검증
- `npm test` 31개 통과(보스 레이드 라이프사이클·저장·타임아웃, 캠프 분산 테스트 추가).
- `tests/browser-smoke.mjs`: 트리 노드 클릭 전직, 보스 소환 → HUD 표시·버튼 비활성·저장 확인 추가. 스크린샷 `boss-closeup.png`, `boss-fight.png`, `tree-after.png`, `hell-terrain.png`.

### 남은 문제 / 다음 후보
- 보스는 한 종류. 도감에 보스 항목 없음. 난이도는 파티 전투력 비례라 저레벨도 시도는 가능하지만 오래 걸린다. 쿨다운 없음(운영금만 소모).
- 보스 처치 후 특별 장비 드랍 없음(현상금·영혼 결정만).
- 전직 트리는 3단계 고정 구조. 스킬별 룬/선행 조건 트리는 아직 없다.

## 2026-09-11 · Claude 2차 작업 (사용자 요청 4건)

요청 원문 요약: (1) 용사 목록에서 "N차" 표기 제거, 한 행에 3명. (2) 장비/전직처럼 용사 팝업에 이미 있는 메뉴는 메인 하단 메뉴에서 제거. (3) 사냥터 화면을 마을 대기 / ACT I~IV 열 + 용사 아이콘만 두고 드래그앤드롭으로 배치. (4) 픽셀이 너무 커서 더 작은 픽셀로 현실감 있게, 단 최대 줌 아웃은 그대로.

### 용사 목록 / 하단 메뉴
- `renderRoster`: 아바타의 `N차` 배지 제거. CSS `.hero-list`는 모든 폭에서 `repeat(3, minmax(0,1fr))`, 모바일은 카드 축소. 20명 = 7행, 관리 패널 안에서 스크롤.
- 하단 메뉴는 맵 · 용사 · 사냥터 · 마을 · 도감 · 소식 · 설정 6+1개. `workshop`/`skills` 뷰 DOM은 남아 있고(팝업 탭이 그 DOM을 옮겨 씀) `setView('workshop'|'skills')`는 용사 팝업의 장비/전직 탭으로 리다이렉트. 대장간 POI 클릭도 팝업 장비 탭.
- 상태 문구는 `statusOf(h)`로 통일 (마을 대기 표시 포함).

### 사냥터 보드 (드래그앤드롭)
- `#expedition-view` 안에 `renderActs()`가 `.zone-board`를 그린다. 열: 마을 대기(`data-drop-zone="-1"`) + ACT I~IV(`data-drop-zone="0..3"`). 각 열 헤더 버튼은 기존 `data-action="zone"`(카메라 이동/선택), 푸터에 액트별 젠 배수 1~5× 버튼과 재료/간격 표시.
- 용사 칩 `.zone-hero[data-hero]`: 초상 캔버스 + 레벨 + 상태 점 + HP 바. 클릭은 `data-action="select"`(팝업 열기), 8px 이상 끌면 드래그로 전환하고 클릭은 `suppressSelect`로 무시. 포인터 이벤트 기반이라 마우스/터치 공통(`touch-action:none`). 드롭 대상은 `elementFromPoint`로 판정, 잠긴 열은 토스트로 거부, 같은 열은 무시.
- 드래그 중에는 기존 `pointerHeld` 가드가 주기적 UI 갱신을 막아 DOM이 바뀌지 않는다. 보드는 markup 비교로 변경 시에만 다시 그린다.
- HTML5 DnD API는 쓰지 않았다(모바일 미지원).

### 마을 대기 (엔진)
- `hero.standby` boolean 추가. `assignZone(s,h,-1)`이 대기 설정: 사냥/이동 중이면 `return`으로 귀환 → 회복 → HP가 차도 `depart`하지 않고 `recover` 상태로 마트에 머문다. 실제 액트 배정 시 `standby=false`, 회복이 끝난 상태면 즉시 `depart`.
- `h.zone`은 대기 중에도 유지(복귀 시 참고용). 몬스터 젠 정원 계산에서 대기 용사는 제외.
- 저장: `standby`는 필드 추가만(version=2 유지). 구 저장은 `restore`에서 `false`로 보정. 새 필드라 마이그레이션 번호 변경 없음.
- 용사 팝업 사냥·기록 탭에도 "마을 대기" 버튼 추가.

### 픽셀 밀도 2배
- 월드 버퍼가 **1 픽셀 = 1 월드 단위**(이전 1 픽셀 = 2 단위). 배경 캔버스 3600×2800, 최대 줌 아웃(minZoom)은 화면에 월드 전체가 들어오는 값이라 이전과 동일. 기본 줌 125%, 최대 줌 4배(이전 2.5배), 홈 125% / 용사 추적 150%.
- 지형(`world-art.js buildTerrain`)은 8단위 타일로 다시 생성(이전 16단위). 디더링·잔디/모래 알갱이·해안·도로·강 모두 새 격자 기준. 사구/용암 균열/능선/정글 웅덩이/부두는 좌표·굵기 2배로 환산. 지형은 페이지당 1회만 생성해 캐시하고(`terrain`), 마을 편집 시에는 소품 목록만 다시 합성한다. 돌길 장식은 배경에 굽지 않고 `flats`로 매 프레임 그린다.
- 손으로 그린 스프라이트(나무·바위·건물·마트·배·장식)는 `pixel-art.js scale2x`(EPX)로 2배 업스케일해 **월드 크기(발자국)는 유지**하면서 계단 현상을 줄였다. 캐시 `hiCache`.
- 용사·몬스터는 원본 해상도 그대로 그려 월드 기준 절반 크기(용사 24×32 단위)가 됐다. 건물/나무 대비 사람 크기가 현실적. 클릭 판정 상자, 이름 라벨 높이, HP 바, 배 오프셋, 이펙트 좌표를 새 스케일로 조정. 스킬 이펙트는 월드 좌표 1:1이라 크기 변화 없음.
- 최종 합성은 `zoom*dpr<1`(축소)일 때만 이미지 스무딩을 켜서 전체 지도에서의 깜빡임을 줄이고, 미니맵 배경도 스무딩.
- 미리보기/초상 캔버스는 영향 없음.

### 검증
- `npm test` 29개 통과(마을 대기/저장 보정 테스트 추가, 기본 줌 1.25 반영).
- `tests/browser-smoke.mjs`: 격리 헤드리스 Whale(`--headless=new --remote-debugging-port=9223 --user-data-dir=<임시>`)에서 통과. 추가 검증: 용사 목록 3열·배지 없음, 하단 메뉴 6개, 실제 마우스 드래그로 칩을 마을 대기 → ACT I 이동/잠긴 ACT IV 거부/클릭 시 팝업, 젠 배수 버튼은 액트 열마다 있으므로 선택자에 `data-zone` 필요. 스크린샷 `/tmp/dungeonmart-review/zone-board.png`, `probe-home.png`, `probe-zoomed.png`.

### 남은 문제 / 다음 후보
- 용사·몬스터 스프라이트는 여전히 24×32 원본이라 4배 줌에서는 픽셀이 보인다. 더 촘촘하게 하려면 캐릭터 시트를 48×64로 다시 그려야 한다(현재 미착수).
- 사냥터 보드에 액트 요약(몬스터 수, 정예 여부)은 없다. 모바일에서는 보드가 가로 스크롤(최소 560px).
- 마을 대기 중인 용사는 마트 앞에 겹쳐 서 있을 수 있다(정렬/휴식 애니메이션 없음).

## 실제 구현 상태 (이전 작업)

### 월드 / 항구

- 3600×2800, WORLD.revision=3. 렌더 버퍼는 1 픽셀 = 1 월드 단위(2차 작업에서 변경). `regionAt`이 좌표/곡선 경계로 하나의 대륙을 구분한다. 원형 섬과 필수 연결 다리는 제거.
- 액트 I: 서쪽~북서쪽으로 길게 이어진 숲/황야/묘지. II: 남부 사구와 왕릉/기둥/오아시스. III: 남동 습지/야자 숲/하천/사원. IV: 동쪽~북동 화산 지대/고사목/용암 균열/봉인문. 경계는 디더링으로 연결, 면적/형태 다름.
- 북부는 숲·해안·작은 산맥을 채우고 제한된 미개척 산악 영역만 남겼다. 가로 전체 공백 없음.
- 서쪽 여명 항구: 영입 직후 4초간 배를 타고 해안 접안점까지 이동 → 부두에서 하선 → 등록소(1.5초) → 마트 → 사냥터. `arrivalStage=-1`은 입항 중. 배 이동 중 물 위 좌표는 정상 상태.
- 주요 건물·바다·미개척 산악지대·밀림 하천·주요 용암 균열 충돌. A* 32 단위 길찾기, 이동/줌/미니맵/용사 추적 유지.

### 마을 꾸미기

- (2026-09-11 변경) 하단 **마을**은 기본으로 **마을 창고**(창고 목록·선택 장비 상세·시설 개선, `gearUI.mountTown`)를 보여준다. 창고 화면의 **마을 편집** 버튼 또는 맵의 **마을 편집** 바로가기(`town-mode` edit)로 꾸미기 모드 진입, **편집 완료**로 창고로 복귀. 편집 동안만 시뮬레이션 잠시 멈춤(기존 일시정지/배속 설정 보존).
- 마트, 회복의 샘, 게시판, 대장간, 창고, 훈련장 6곳을 선택 → 지도 새 위치 클릭/터치하여 이동. 32 단위 스냅. 건물 외곽/편집 경계/배치 가능·불가 표시.
- 돌길, 가로수, 화단, 벤치, 가로등 무료 배치/장식 지우기. 최대 300개, 동일 칸 새 장식은 교체. 장식/돌길은 미관용이고 충돌을 추가하지 않음.
- 주요 도로 침범, 건물 겹침, 마을 경계 밖 배치 거부. 성공 즉시 저장. 최근 20회 되돌리기(세션 내), 기본 배치 복원 가능.
- 마트 이동 시 실제 귀환/회복/부활 목표도 이동. 건물 충돌/길찾기 캐시와 배경 재생성. 건물 아래에 있던 용사는 안전한 위치로 이동.
- 대장간/창고는 기존 장비 제작·재고 화면으로 연결, 훈련장은 스킬 미리보기로 연결. 별도 생산/물류 시뮬레이션을 추가한 것은 아님.

### 용사 / 스킬

- 용사 팝업 탭: 정보·외형 / 장비 / 전직·스킬 / 사냥·기록. 장비/전직은 이 팝업에서만 관리(하단 메뉴에서 제거됨). 장비 제작·구매·장착·분해, 코스튬, 전직·스킬 강화/초기화, 사냥터 배정이 팝업 안에서 동작.
- 기존 workshop/skills DOM을 팝업으로 이동시켜 사용하므로 중복 ID/이벤트 없음. 닫으면 원위치로 반환. 전직 확인은 기존 확인 dialog를 위에 띄우고 완료 후 용사 팝업 유지.
- 이전/다음 용사 전환. **이 용사 따라가기**는 팝업을 닫고 해당 용사를 지속 추적. 맵 드래그/줌/바로가기는 추적 해제.
- 5 기본 직업 / 10 2차 / 20 3차, 스킬 총 100개. 스킬 아이콘은 SVG. 전직 여부와 무관하게 선택한 기본 직업의 모든 계열을 미리 볼 수 있음. 스킬 트리는 카드 계층 구조.
- 실제 전투와 미리보기에서 공유하는 `drawSkillEffect`: 화염/유성, 얼음, 번개, 출혈, 독, 소환, 성역, 보호막, 공간, 화살, 베기, 지진, 회전, 함성, 저주, 시체 폭발 연출.
- 미리보기: 독립 훈련 허수아비 캔버스, 반복 재생/다시 보기/멈춤/0.5배속. 재료·스킬 포인트·전직 변경 없음. 데스크톱은 트리 옆 고정, 모바일에서 스킬 클릭 시 미리보기로 스크롤. **전투 수치 시뮬레이터는 아니며, 패시브는 보조 효과를 오라로 표현한다.** 설명에 구분 표시.
- 용사 ID 기반 별칭/출신/작은 외형 변형, 직업 문양, 전직 계열별 기본 색상/장식. 별칭·출신은 외형/서사 요소이며 추가 랜덤 능력치가 아님.

## 기존 게임 기능

- 장비 제작·구매·장착·이전·분해, 시설 개선, 저장 입출력. 최종 액티브 6 + 패시브 2.
- 영입 120 G. 직업 각각 20%, 등급 독립: 노멀 60 / 매직 25 / 레어 12 / 전설 3%. 기본 능력치 배율 1 / 1.2 / 1.45 / 1.8. 기본 시작 용사 5명 노멀.
- 액트별 몬스터 젠 1~5배. 1.8초/배율. 일반 시간 배속 별개. 누르고 있는 버튼이 주기적 UI 갱신으로 없어지지 않도록 보호.

## 파일과 구조

- `src/world.js`: 정적 대륙, POI, 기본 마을, 배치 검증, 활성 마을 레이아웃과 revision.
- `src/world-art.js`: 대륙/마을/부두/선박/장식 픽셀 아트. `src/navigation.js`: 동적 레이아웃 revision에 따른 그리드/경로 무효화.
- `src/engine.js`: 게임 상태, 전투/스킬 VFX 이벤트, 입항, editTown, 저장 마이그레이션.
- `src/skill-visuals.js`: 전체 스킬 목록, 아이콘, 효과 계열/그리기, 용사 별칭/계열 색상. 순수 캔버스 함수, DOM/게임 상태 변경 없음.
- `src/render.js`: 월드/미니맵, 동적 배경, 편집 그리드/배치 미리보기, 선박/스킬 이펙트. `src/pixel-art.js`: 기본 픽셀 스프라이트와 개체 변형.
- `src/app.js`, `index.html`, `style.css`: HUD/하단/팝업 내부 탭/스킬 스튜디오/마을 도구/추적.
- `tests/town.test.mjs`: 신규 지형/마을/저장/스킬 목록 테스트. `tests/world.test.mjs`: 대륙 경로와 해상 입항 반영.

## 저장 호환 / 주의

- localStorage 키 `dungeon-mart-save-v1`, 내부 version=2 유지. **worldRevision=3** 및 **town:{buildings:{},decorations:[]}** 추가. 2차 작업에서 용사별 **standby**(boolean) 추가, 없으면 false.
- 이전 v1 또는 worldRevision 없는 기존 v2는 용사·몬스터 위치를 새 지역/마을의 안전 좌표로 변환. 금액·재료·레벨·전직·스킬 투자·장비·등급 보존. 진행 중 전투 대상/소환수/시체는 좌표 변경 시 정리.
- town 없는 저장은 기본 배치. 잘못된 배치/장식/범위/미래 revision 거부. `restore`가 다른 저장을 읽기만 하는 동안 현재 마을을 변경하지 않도록 활성 레이아웃을 복원한다. 실제 게임 적용 시 applyTownLayout 호출.
- 운영금: 2026-09-11 사용자 요청으로 새 게임 시작 운영금 0 G. 이전의 일회성 10,000 G 보정은 종료(플래그만 기록, 기존 저장 잔액 유지).
- 사용자의 개인 브라우저/저장은 직접 접근하지 않았다. 새로고침 시 마이그레이션 적용. 테스트는 격리 프로필만 사용.

## 실행·검증

- `/Users/nykim/Code/00.Project/DungeonMart`에서 `npm start` → http://localhost:4173. 추가 패키지 없음, Node 22+.
- `npm test`: **45개 통과**. 기존 경제/전직/장비/저장, 자연 지형/경로, 해상 입항, 건물 이동/충돌/귀환 목표/되돌리기/장식, 구 저장 보존, 읽기 전용 불러오기, 100개 스킬 목록 검증.
- `node tests/browser-smoke.mjs`: **격리된 Whale DevTools 9223 전용**, 이 프로필 저장을 초기화. 개인 브라우저에 연결 금지. 헤드리스 실행 예: `/Applications/Whale.app/Contents/MacOS/Whale --headless=new --remote-debugging-port=9223 --user-data-dir=/tmp/whale-test --no-first-run --disable-gpu --window-size=1512,982 about:blank` (서버 `npm start` 필요).
- 브라우저 테스트를 실제 화면에 보이는 컨트롤의 포인터 클릭으로 개편. 통과: 4개 액트/줌/팬/모바일 핀치, 팝업 내부 장비·전직·스킬, 미습득 미리보기/정지/재생, 모든 100개 VFX 그리기, 움직이는 용사 추적, 실제 지도 좌표로 마트 이동/화단·길 배치/undo/새로고침 복원, 항구 영입, 젠 배수 길게 누르기, 구 저장 migration, PC·모바일 넘침/모바일 미리보기 노출.
- 스크린샷 `/tmp/dungeonmart-review/`: `final-continent.png`, `act-1.png`~`act-4.png`, `town-moved.png`, `harbor-arrival.png`, `profile-overview.png`, `profile-skill-tree.png`, `preview-meteor.png`, `mobile-skills.png`, `mobile-town.png` 등.

## 장비 시스템 v2 (2026-09-11 구현, 브랜치 feature/equipment-v2)

설계 원문은 `docs/EQUIPMENT_DESIGN.md`. 아래는 실제 구현 상태이며 설계와 다른 부분을 명시한다.

- **데이터 `src/items.js`**: 베이스 37종(설계 42종 중 일부 통합), 등급 일반/매직/레어/세트/유니크, 앞말 13군·뒷말 20군(T1~T8, ilvl 잠금), 레어 이름표(20×7×3), 보석 7종×5품질, 각인석 30종(6계열), 진언 20종, 직업 세트 5종×6부위, 유니크 24종, 격자 프리셋(W3~W12 / A6~A12 / 방어구 조합, 총 40칸), 생성기 `generateItem`, 격자 규칙 `fits/findSpot/activation`.
- **엔진 `src/engine.js`**: `s.items`(사전) + `s.warehouse`(id 목록, 용량 80→192칸은 창고 시설 레벨) + `s.drawer`(보석·각인석 스택) + `s.fieldDrops`(빛기둥) + `s.pity`(소프트 천장) + `s.codex`. 용사는 `grid/placed/bagItems`. `statsOf`는 `gearBonus`(활성 판정 → 접사·박음돌·진언 → 세트 보너스, 서명 캐시)를 합산하고 무게 페널티(100~120% 속도 -10%, 120~150% -25%, 150% 초과 배치 불가)를 적용. 드롭: 일반 12%/정예 1~2개, 보석 4%/정예 25%, 각인석 1.5%/정예 15%, 세트·유니크는 필드 빛기둥(클릭 획득, 게임 시간 300초 후 자동 입고, 최대 20개). API: `placeItem/autoPlace/unplaceItem/rotateItem/salvage/craft(base,grade,tier)/insertSocket/combine(runeUp|gemUp|punch|clear|reroll|upgrade|tierUp)/pickupFieldDrop/upgradeMart('warehouse')`. 첫 배치 시 구매(용사 골드 → 운영금, `price%` 반영). 몬스터 속성 공격(독/번개/냉기/화염)에 저항 적용, 아이템 `revive`로 1회 부활(재사용 600초), 치명타 피해 `critDmg`, 무기 속성 피해와 냉기 둔화·독 지속.
- **세트·유니크·진언 특수 효과는 기존 패시브 키(damage/defense/boss/execute/capacity/dot/double/onkill/revive 등)와 능력치로 매핑**했다. 설계의 서술형 효과(관통, 오라 범위 등) 일부는 대응 능력치로 대체했고 툴팁에 실제 효과를 표시한다.
- **UI `src/gear-ui.js`**: 용사 팝업 장비 탭. 좌측 무기칸/방어구칸/장신구칸 격자(드래그 앤 드롭, 격자 안 이동, 창고 목록에 놓으면 빼기, 드래그 중 R키 회전, 초록/빨강 미리보기, 비활성 빗금과 사유), 우측 창고(필터·정렬·배치 버튼), 하단 선택 장비 상세(접사·홈·세트 진행·유니크 설명·조합 버튼), 박음돌 서랍(선택 후 박기, 3개 합성, 진언 목록/발견), 대장간 제작(종류·베이스·등급·단계, 용사 격자에 맞는지 ✓ 표시), 시설(제작대·회복·창고). 모바일은 한 열, 터치에서는 창고 행 드래그 대신 배치 버튼.
- **지도**: 빛기둥(세트 초록/유니크 황금) + 이름·남은 시간 라벨 + 미니맵 점 + 지도 왼쪽 HUD 배지(클릭 시 카메라 이동). 기둥 클릭·호버 툴팁, 획득 시 아이템 카드 모달과 "장비창 열기".
- **외형**: 활성 무기 베이스 glyph, 몸 방어구 armorLevel(+세트/유니크/진언 1단계), 장신구 등급 오라 색.
- **설계와의 차이(미구현)**: 하역장 없음(창고 가득 차면 자동 분해·로그), 창고는 격자가 아닌 용량제(자동 정리), 보옥 없음, 반지·목걸이 도박/세트 조각 교환 없음, 떠돌이 상인·미감정·게시판 의뢰·심연 층·레벨 99·도감 화면(진언 목록만) 없음, 홈 추가 접두사 없음, 용사 등급 ★ 표기는 그대로.
- **저장 v3**: `restore`가 version 1·2를 `migrateItems`로 변환(슬롯 장비 → 베이스 매핑: 무기는 직업 기본 무기, 방어구는 가죽/사슬/사슬/판금, 액세서리는 목걸이. `stats`를 `implicit`에 보존, 전설은 유니크 색 "옛 시대의 ○○"). 장착 무기가 들어가도록 무기칸을 확장하고 자동 배치, 나머지는 창고. `validateItems`가 위치 중복·격자 겹침·잘못된 박음돌을 거부한다. `_`로 시작하는 캐시 필드는 저장에서 제외.
- **검증**: `npm test` 37개 통과(`tests/items.test.mjs` 8개 신규). 격리 헤드리스 Whale(9224, 임시 프로필)로 제작→배치→드래그 앤 드롭→각인석 박기→빛기둥 배지→기둥 클릭 획득→모달까지 실제 포인터로 확인, 콘솔 오류 없음. 모바일 420px 레이아웃 확인. `tests/browser-smoke.mjs`를 새 장비 UI(세 격자, 제작→배치)와 운영금 0 시작에 맞춰 갱신했고 격리 헤드리스 Whale(9223)에서 통과.
- **동시 작업 주의**: 이 작업은 별도 워크트리 `../DungeonMart-equip`(브랜치 feature/equipment-v2)에서 진행했다. 같은 시간에 다른 세션이 main 작업 트리에서 보스 레이드(`BOSS_TYPE`, `BOSS_LAIR`, `raid`, `summonBoss`)를 미커밋 상태로 수정 중이었다. main 머지 시 `engine.js`(statsOf/hurtHero/kill/restore), `app.js`(import·renderDetail·click), `data.js`, `render.js`, `index.html`, `style.css`에서 충돌이 예상되며, 장비 쪽은 `RARITIES/SLOT_NAMES/inventory/equipment`를 전부 제거했으므로 보스 코드가 이를 참조하면 새 API로 바꿔야 한다.

## 2026-09-11 후속: 통합 인벤토리, 제작소 분리, 보스 쿨다운·전리품, 분해, 지도 아이콘 바

- **통합 인벤토리**: 용사 팝업 장비 탭의 격자를 한 틀(`.inventory-frame`) 안의 무기·방어구·장신구 구간(`.inventory-region`)으로 표시. 데이터 모델(`hero.grid/placed`, zone·x·y)은 그대로. 구간 정보는 틀 아래 한 줄. 창고 목록은 그 용사가 쓸 수 있는 장비만 표시하고 "사용 불가" 표기는 없앰. 배치하면 창고에서 빠지고, "해제 · 창고로"로 되돌아간다.
- **마을 제작소**: 하단 메뉴 `제작소`(view `workshop`, `#workshop-view`, 대장간 POI 클릭도 여기로). 제작 폼(✓ = 선택 용사 사용 가능), 창고 전체 목록과 개별 분해, **전체 분해**(일반/매직/레어 버튼, `salvageAll`), 선택 장비 상세·조합·박음돌 박기, 서랍, 시설. 용사 장비 탭(`#gear-view`)에는 배치·해제·회전만 남김. `gear-ui.js`는 `installGearUI(host, ctx, 'hero'|'workshop')` 두 인스턴스.
- **유물의 정수**(`materials.relic`, `MATERIALS.relic`): 세트·유니크 분해 시 1개. 아직 소비처 없음(사용자 계획: 옵션 수치·옵션 종류 재조정에 사용). 구 저장은 relic 0으로 보정.
- **보스**: `RAID_COOLDOWN`=1200(게임 시간 20분, `s.raidReadyAt`), 소환 버튼에 남은 시간 표시. 처치 시 `bossLoot`: 3개 빛기둥, 개당 세트 15%/유니크 10%(`DROP_TABLE.boss`), 3연속 꽝이면 확정(`s.bossPity`), 첫 처치는 유니크 확정, ilvl 45+2×처치수. 정예 피해 옵션이 보스에도 적용.
- **지도 상단 바로가기**: 헤더 아래 왼쪽에 아이콘 버튼(마을, ACT I~IV — 잠긴 액트는 자물쇠)과 전체 지도·헌터 추적·마을 꾸미기·보스 소환. `#zone-shortcuts`는 `renderRaid`에서 갱신. 빛기둥 배지는 그 아래.
- **테스트**: `npm test` 40개(보스 쿨다운·전리품·전체 분해 추가). `tests/browser-smoke.mjs`는 `DM_DEVTOOLS`/`DM_APP` 환경 변수로 포트 변경 가능(다른 세션과 동시 실행용). 격리 헤드리스 Whale에서 통과. **주의: 여러 세션이 헤드리스 브라우저 포트(9223/9224)를 공유하면 페이지가 서로 바뀐다. 세션마다 다른 포트·프로필을 쓴다.**

## 2026-09-18 · 전직 기반 최대 레벨

- `levelCap(h)`: 미전직 20, 2차 전직 후 40, 3차 전직 후 60. 상한에서 `h.xp=0`으로 고정하고 `capNotified`로 한 번만 로그. 전직 카드는 기존대로 Lv.20/40 조건. 구 저장에서 상한을 넘긴 용사(예: 미전직 Lv.35)는 강등하지 않고 그 자리에서 멈춘다.

## 남은 범위와 다음 작업

- **모바일 터치 전환(최우선)**: `docs/MOBILE_UI.md` 8절 감사 목록 순서대로. 맵 툴팁 탭/롱프레스화 → `title` 의존 제거 → 탭 타깃 44px 전수 확대 → 마을 편집 고스트 터치화 → 폰에서 좌측 레일을 하단 메뉴로 통합 → 파괴적 동작 확인 단계 → 안내 문구 터치 용어로 통일. 각 항목 완료 시 감사 목록에서 지운다.
- **장비 v2 후속**: feature/equipment-v2를 main에 머지(보스 레이드 변경과 충돌 해결), `tests/browser-smoke.mjs`를 새 장비 UI로 갱신, 드롭·가격·무게·접사 수치 밸런스, 미구현 항목(하역장, 보옥, 상인, 의뢰, 심연 층, 도감 화면, 레벨 99) 순차 구현.

- 사용자가 요청한 이번 범위는 구현. 다음 실제 플레이 피드백에 따라 자연 지형 디테일/마을 편집 UX/스킬 연출을 보완할 것.
- 보스·침공·독립 생산·물류·경제 확장, 오프라인 보상은 미구현. 몬스터/용사 등급/지역 이동거리의 장기 밸런싱 필요.
- 나무/바위 등 모든 장식이 충돌체인 것은 아님. 기본 주요 도로는 유지되며 사용자가 놓는 돌길은 외관용. 마을 건물 추가 구매/회전 기능은 없음(기존 6개 이동 + 5종 장식).

## 2026-09-19 · 전직 트리 아이콘 정리

- 전직 노드의 첫 스킬 아이콘 재사용 제거. src/class-icons.js 직업별 도끼/활/해골/지팡이/방패 등 채움형 문양 적용. 스킬 아이콘 무의미한 점 장식 제거·선 굵기/색 대비 개선. 노드 문양 44px·글자 확대.
- src/app.js, src/skill-visuals.js, style.css 변경. 문법/diff 검사 및 /tmp/tree-icons-check.mjs 격리 390×844/360×640 노드 7개·넘침/오류 없음 확인. 로컬만, 저장·전직 로직 변경 없음.
