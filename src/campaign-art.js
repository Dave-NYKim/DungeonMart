let buildings,bosses;export let campaignArtRevision=0;const cache=new Map();
export const campaignArtStatus=()=>({buildings:!!buildings,bosses:!!bosses});
if(typeof Image!=='undefined')for(const [name,set] of [['buildings',v=>buildings=v],['act-bosses',v=>bosses=v]]){const img=new Image();img.onload=()=>{set(img);campaignArtRevision++;cache.clear();};img.src=new URL(`../assets/${name}/sprites.png`,import.meta.url);}
export function buildingSprite(p){const index=['mart','crypt','tomb','temple','fortress','final-seal'].indexOf(p.type);if(!buildings||index<0)return null;const key=`building:${p.type}:${p.width}:${p.height}`;if(cache.has(key))return cache.get(key);const c=document.createElement('canvas');c.width=Math.ceil(p.width/2);c.height=Math.ceil(p.height/2)+8;const ctx=c.getContext('2d');ctx.imageSmoothingEnabled=false;const w=buildings.naturalWidth/3,h=buildings.naturalHeight/2;ctx.drawImage(buildings,index%3*w,Math.floor(index/3)*h,w,h,0,0,c.width,c.height);cache.set(key,c);return c;}
export function actBossSprite(index){if(!bosses)return null;const key=`boss:${index}`;if(cache.has(key))return cache.get(key);const c=document.createElement('canvas');c.width=c.height=128;const ctx=c.getContext('2d');ctx.imageSmoothingEnabled=false;const w=bosses.naturalWidth/2,h=bosses.naturalHeight/2;ctx.drawImage(bosses,index%2*w,Math.floor(index/2)*h,w,h,0,0,128,128);c.anchorY=120;cache.set(key,c);return c;}
