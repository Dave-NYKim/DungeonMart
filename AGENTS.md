# Dungeon Mart 작업 규칙

- 대화에서 사용자가 정한 게임 방향과 우선순위를 먼저 따른다.
- 새 작업을 시작할 때 `HANDOFF.md`를 읽고 현재 상태와 미완료 항목을 확인한다.
- 작업을 완료할 때마다 `HANDOFF.md`를 갱신한다. 다른 에이전트(특히 Claude)가 대화 기록 없이 이어받을 수 있도록 요청, 구현 결과, 변경 파일, 실행·검증 방법과 결과, 남은 문제, 다음 작업을 적는다.
- 기능의 실제 구현과 향후 설계를 구분한다. 미구현 기능을 완료로 기록하지 않는다.
- 완료 이력은 `docs/WORKLOG.md`에 날짜와 함께 추가한다. 사용자의 기존 저장 데이터를 보존하고, 저장 구조 변경에는 마이그레이션을 제공한다.
- 추가 패키지 없는 로컬 웹 게임 구조를 유지한다. `npm start`로 실행하고 `npm test`로 로직을 검증한다.
- `tests/browser-smoke.mjs`는 DevTools 9223의 격리된 테스트 프로필 전용이다. 개인 브라우저에 연결하지 않는다. 해당 테스트는 테스트 프로필의 저장을 초기화한다.
