# 작업 이력

## 2026-09-10 · 첫 플레이어블

- 자동 사냥·귀환·회복·부활, 최대 20명 영입, 5개 기본 직업과 20개 최종 전직 구현.
- 공용 재료와 개인 골드 경제, 장비 제작·구매·장착·이전·분해, 시설 업그레이드 구현.
- 디아블로 II 액트별 대표 몬스터 16종을 자체 그래픽과 수치로 구현.
- 브라우저 저장·파일 내보내기/불러오기, 11개 로직 테스트와 UI 스모크 테스트 작성.

## 2026-09-10 · 픽셀 아트 개편

- 용사 24×32 프레임, 장비·코스튬 레이어와 걷기·공격 포즈 구현.
- 몬스터·마트·나무·지형 및 UI 아이콘을 자체 픽셀 아트로 재구성.
- 용사 목록·프로필·도감에 동일한 스프라이트 사용.
- 기존 게임 기능과 데스크톱/모바일 레이아웃 검증.

## 2026-09-10 · 대형 월드 / 카메라 / 헌터 입장

- 사용자 요청: 작은 전체 화면 지도 대신 지역 중심 확대 화면, 줌·드래그, 액트 간 충분한 거리, 고용 헌터의 입장 장소, 미래 기믹을 위한 맵 구조.
- 3600×2800 월드, 중앙 마트+4개 독립 지역, 협곡·연결 다리와 지역별 3개 몬스터 캠프 구현.
- POI 데이터에 거점 건물과 확장 부지·유적을 정의. 실제 기능과 예약 부지를 구분.
- 기본 100% 카메라, 전체 보기, 휠·핀치·드래그, 미니맵, 헌터 추적, 지도 넓게 보기 구현.
- 헌터 도착장→등록→마트 입장 상태와 A* 이동 경로 구현. 다리 입구의 경로 정지 문제를 회귀 테스트로 수정.
- 저장 버전 1→2 변환. 기존 용사·전직·장비·재료·골드 보존.
- 인계 규칙 AGENTS.md, 현재 상태 HANDOFF.md, 상세 설계 docs/MAP_DESIGN.md 추가.
- 최종 검증 결과는 HANDOFF.md에 기록.

## 2026-09-10 · 무작위 영입 / 용사 등급 / 액트별 젠 배율

- 직업 선택 영입을 5직업 동일 확률 무작위 영입으로 변경. 120 G 및 최대 20명 유지.
- 노멀/매직/레어/전설, 흰색/파랑/노랑/빨강 표시와 기본 공격·방어·체력 배율 구현. 확률과 수치는 HANDOFF.md 및 HERO_GRADES 참고.
- 각 액트 독립 1~5배 젠 선택 UI와 저장 구현. 전체 게임 시간과 몬스터 정원은 유지.
- 기존 저장에 없는 등급/젠 설정 기본값 복원, 잘못된 수치 거부.
- 로직 테스트 19개 통과. 브라우저 테스트에 랜덤 영입 및 액트별 배율 저장/복원 추가.
- 앞선 맵 개편 브라우저 검증도 완료: 휠·드래그·미니맵·POI·전체 보기·모바일 핀치·입장·기존 게임 흐름 통과.
- 최신 브라우저 통합 검증 통과: 무작위 영입과 ACT III 5배 설정 및 새로고침 복원 포함.

## 2026-09-10 · 몬스터 젠 배수 입력 오류 수정 / 테스트 시작 자금

- 주기적 UI 갱신이 조작 중인 입력 요소를 교체하는 문제 수정. 몬스터 젠 설정을 1~5배 버튼 및 생성 간격 표시로 개선.
- 실제 마우스를 1.5초 누르는 회귀 검증, 액트별 설정 저장/복원, 모바일 레이아웃과 기존 UI 테스트 통과.
- 사용자 테스트 요청으로 새 게임 시작 운영금 10,000 G 적용. 기존 저장 잔액 보존.
- 로직 테스트 20개 통과. HANDOFF.md에 원인과 후속 작업 맥락 기록.

## 2026-09-11 · 맵 상시 표시와 하단 관리 UI

- 사용자 요청으로 좌우 고정 패널을 하단 메뉴/접이식 관리 패널과 큰 용사 정보 팝업으로 재배치. 맵과 사냥은 모든 관리 화면에서 계속 표시. 운영금·재료·저장·시간 조작을 맵 내부 HUD로 이동. 모바일 대응.
- 기존 저장 운영금을 일회 10,000 G로 맞추는 operatingGrantApplied 마이그레이션 추가. 나머지 진행 보존, 이후 지출 유지. 개인 브라우저 저장 직접 조작 없음.
- 변경: index.html, style.css, src/app.js, src/engine.js, tests/recruitment.test.mjs, tests/browser-smoke.mjs 및 인계 문서.
- 검증: npm test 21개 통과. 격리 테스트 프로필의 browser-smoke 통과, 하단 메뉴별 맵 가시성/팝업 Esc/모바일 화면 추가 확인. 스크린샷 /tmp/dungeonmart-review/.

## 2026-09-11 · 연결형 대륙, 마을 편집, 용사 내부 관리와 스킬 시연

- 요청: 원형 4개 사냥터를 이어진 자연 지형으로 변경, 항구/마을/액트의 상대 위치 반영. 후속 정정에 따라 격자/동일 크기/북쪽 전체 공백을 피하고 액트 I을 북서쪽으로 확장, 북부 숲·산맥의 일부만 확장 공간으로 확보.
- 구현: 비대칭 대륙의 숲·사구·습지·화산 지대와 부두. 영입 용사 선박 입항→하선→등록→마트. 동적 마을 건물 6개 이동, 5종 장식 배치/삭제, 충돌·주요 도로 보호, 20회 undo/기본 배치, 즉시 저장, 이동한 마트로 귀환/회복.
- 용사 팝업 내부 탭에서 장비/전직/스킬/코스튬/사냥 배정/기록 조작, 이전·다음 용사, 따라가기. 100개 스킬 아이콘과 미전직 스킬 미리보기, 재생/정지/0.5배속. 전투와 시연의 효과 그리기 공유. 직업/전직 색상·문양, 개체 별칭·출신·작은 외형 변형.
- 저장: version=2 유지, worldRevision=3 및 town 추가. 기존 재화·장비·성장 보존하고 공간 좌표만 마이그레이션. 취소한 저장 불러오기가 활성 마을에 영향을 주지 않도록 읽기 부작용 제거. 개인 저장 직접 접근 없음.
- 변경 파일: src/world.js, src/world-art.js, src/navigation.js, src/engine.js, src/render.js, src/pixel-art.js, 신규 src/skill-visuals.js, src/app.js, index.html, style.css, tests/world.test.mjs, 신규 tests/town.test.mjs, tests/browser-smoke.mjs, HANDOFF.md, docs/MAP_DESIGN.md, docs/WORKLOG.md.
- 검증: npm test 28개 통과. 격리 브라우저에서 실제 포인터로 마을 편집/undo/새로고침, 팝업 안 장비·전직·스킬, 미습득 시연/정지/재생, 모든 100개 효과 그리기, 추적, 입항, 저장 migration, PC/모바일 UI 검증 통과. 결과물 /tmp/dungeonmart-review/.
- 범위 구분: 장식 돌길은 외관용. 대장간/창고/훈련장은 기존 관리와 시연 진입점이며 독립 생산·경제를 추가한 것은 아님. 시연은 공유 VFX 기반 연출이고 피해 수치 시뮬레이터는 아님. 보스·침공·오프라인 보상은 기존 미구현 상태.

## 2026-09-11 · 장비 시스템 v2 설계 (문서만)

- 사용자 요청: 등급 일반/매직/레어/세트/유니크, 원작과 다른 이름의 룬워드형 조합, 마을 창고 → 용사 격자 장비창(놓으면 즉시 효과), 큰 장비=큰 칸, 무게, 무기·방어구·장신구 구역의 용사별 무작위 크기, 접두사/접미사 이름, 무한 플레이.
- 결과: docs/EQUIPMENT_DESIGN.md 작성. 각인석(룬) 30종·진언(룬워드) 20종·세트 9·유니크 24·베이스 42·접사 8단계·격자 프리셋·무게·창고·심연 층·저장 v3 마이그레이션·구현 5단계 정의.
- 구현 없음. 테스트 변동 없음. HANDOFF.md에 설계 상태와 다음 작업 반영.

## 2026-09-11 · 용사 UI 정리, 사냥터 드래그앤드롭, 픽셀 밀도 2배 (Claude 인계)

- 요청: 용사 목록에서 "N차" 제거·한 행 3명, 장비/전직 메뉴를 하단에서 제거(팝업에 이미 있음), 사냥터를 마을 대기/ACT I~IV 열에 용사 아이콘을 드래그앤드롭으로 배치, 더 작은 픽셀로 현실감(최대 줌 아웃 동일).
- 구현: 3열 용사 그리드, 하단 메뉴 6개(맵·용사·사냥터·마을·도감·소식)+설정. `setView('workshop'|'skills')`와 대장간 POI는 용사 팝업 탭으로 연결. 사냥터 보드 `.zone-board`(포인터 이벤트 드래그, 터치 지원, 잠긴 액트 거부, 열별 젠 배수). 엔진 `hero.standby`와 `assignZone(s,h,-1)`로 마을 대기, 구 저장은 false 보정.
- 렌더: 월드 버퍼 1 픽셀 = 1 월드 단위. 지형 8단위 타일 재생성(1회 캐시), 소품/건물/배/장식은 Scale2x로 발자국 유지, 용사·몬스터는 원본 해상도(월드 기준 절반 크기). 기본 줌 125%, 최대 4배, 축소 시 스무딩. 돌길 장식은 flats로 프레임마다 합성.
- 변경 파일: src/engine.js, src/app.js, src/render.js, src/world-art.js, src/pixel-art.js(scale2x), src/camera.js, index.html, style.css, tests/engine.test.mjs, tests/world.test.mjs, tests/browser-smoke.mjs, HANDOFF.md, README.md, docs/WORKLOG.md.
- 검증: npm test 29개 통과. 격리 헤드리스 Whale에서 browser-smoke 통과(실제 마우스 드래그로 마을 대기↔ACT I 이동, 잠긴 ACT IV 거부, 칩 클릭 시 팝업, 3열 목록, 메뉴 6개, 모바일 레이아웃). 스크린샷 /tmp/dungeonmart-review/.
- 미구현: 캐릭터 시트 자체의 고해상도화(48×64), 보드의 액트 요약 정보, 대기 용사 정렬.

## 2026-09-11 · 장비 설계 정정: 필드 빛기둥, 직업 세트

- 사용자 정정: 세트·유니크는 필드 드롭에서만 간간히, 디아블로 III식 빛기둥 표시, 클릭 획득, 5분 미획득 시 자동 창고 입고. 세트는 직업당 하나만.
- 문서 반영: docs/EQUIPMENT_DESIGN.md 4장 드롭 확률·소프트 천장, 4.1 빛기둥/클릭/자동 입고/저장 구조, 7장 직업 세트 5종 6부위 + 단계 성장, 13·14·15·17·18장 출처·저장·코드 매핑 갱신. 공용 세트 4종 삭제, 상인·도박에서 세트·유니크 제거.
- 구현 없음. 저장소 첫 커밋 생성.

## 2026-09-11 · 보스 레이드, 몬스터 분산 스폰, 지옥 지형 정리, 클릭형 전직 트리 (Claude)

- 요청: 보스 소환 버튼 → ACT IV에 악마 출현, 전체 용사가 이동해 공동 처치, 디아블로를 따라하지 않은 자체 픽셀 디자인. 몬스터가 액트 한곳에만 몰리는 문제. 불타는 지옥의 뭉친 생성물 제거. 전직을 디아 2 스킬 트리 느낌의 트리로, 클릭 즉시 전직.
- 구현: 엔진 `summonBoss`/`s.raid`(500 G, 240초 제한, 잿불 폭발·부하 소환, 승리 시 현상금 2,000 G·영혼 결정 3·XP/골드), 레이드 중 전 용사 ACT IV 집결 후 원래 배정 복귀. 원본 보스 스프라이트 64×72 2프레임, 보스 HUD·이름 배지·미니맵 마커. 스폰 캠프를 액트당 7~10곳으로 분산. 지옥 무작위 용암 줄무늬 제거·소품 감소·팔레트 저대비. `.d2-tree` 트리 UI와 `tree-node` 클릭 전직.
- 변경 파일: src/engine.js, src/data.js, src/world.js, src/world-art.js, src/pixel-art.js, src/render.js, src/app.js, index.html, style.css, tests/engine.test.mjs, tests/browser-smoke.mjs, HANDOFF.md, README.md, docs/WORKLOG.md.
- 검증: npm test 31개 통과. 격리 헤드리스 Whale에서 browser-smoke 통과(트리 클릭 전직, 보스 소환 HUD/저장). 레벨 40 파티 실전에서 74초 처치 확인.

## 2026-09-11 · 장비 시스템 v2 구현 (feature/equipment-v2)

- 요청: 설계(docs/EQUIPMENT_DESIGN.md)대로 구현. 커밋하면서 진행.
- 구현: `src/items.js`(베이스 37·접사·보석·각인석 30·진언 20·직업 세트 5·유니크 24·격자 프리셋·생성기·격자 규칙), `src/engine.js`(격자 장비창 배치/활성/무게, 드롭과 빛기둥, 창고 용량, 홈/박음돌/진언, 조합 7종, 제작 개편, 저장 v3 마이그레이션·검증), `src/gear-ui.js`(장비 탭 전체 UI, 드래그 앤 드롭), `src/app.js`·`src/render.js`·`index.html`·`style.css`(빛기둥, 배지, 획득 모달, 외형 연동, 스타일).
- 테스트: 기존 테스트를 새 API로 수정, `tests/items.test.mjs` 추가. 37개 통과. 격리 헤드리스 브라우저로 제작·배치·드래그·박기·빛기둥 획득 확인.
- 설계와 다른 점과 미구현 항목은 HANDOFF.md "장비 시스템 v2" 절에 기록. 다른 세션이 main 작업 트리에서 동시 작업 중이라 워크트리로 분리해 진행했고 main 머지는 남아 있다.

## 2026-09-11 · 장비 v2 main 병합, 운영금 0 시작, 스모크 테스트 갱신

- feature/equipment-v2를 main의 보스 레이드 커밋과 병합(engine/app/render/style/test/docs 충돌 해결). `npm test` 39개 통과.
- 사용자 요청으로 새 게임 운영금 0 G. 구 저장의 일회성 10,000 G 보정 종료(잔액 유지). 관련 테스트·README·HANDOFF 갱신.
- `tests/browser-smoke.mjs`를 격자 장비창 기준으로 수정(제작→배치→격자 아이템 확인, 영입 전 운영금 주입). 격리 헤드리스 브라우저에서 통과.

## 2026-09-11 · 오프라인 재료 보상 (Claude)

- 요청: 게임을 꺼 둬도 재료를 모아 오는 기능. 시뮬레이션 대신 실제 귀환 입고량으로 측정한 분당 수익률로 정산하는 방식으로 합의.
- 구현: 엔진 `s.yield`(120초 창 이동평균), `settleOffline`(60% 효율, 최대 8시간, 1분 미만·레이드 중 제외, 재료만). 앱은 로드 시 `savedAt`, 탭 복귀 시 `hiddenAt` 기준으로 정산하고 모달로 안내. 구 저장은 rate 0에서 시작.
- 변경 파일: src/engine.js, src/app.js, tests/engine.test.mjs, HANDOFF.md, README.md, docs/WORKLOG.md.
- 검증: npm test 40개 통과, 격리 브라우저에서 모달/재료 증가/중복 지급 없음 확인, browser-smoke 통과.

## 2026-09-11 · 난이도 사다리 (Claude)

- 요청: 노멀/나이트메어/헬 × 10단계, 노멀 10 ≪ 나이트메어 1, 액트 간 완만한 차이, 5분 변경 잠금, 보스 처치로 다음 난이도 해금, 고난이도 몬스터는 임시 네모 박스.
- 구현: `DIFFICULTIES`(배율 1/8/60), 단계당 +30%, 액트 계수 1~2.2. 엔진 `zoneStats`/`setDifficulty`/`s.difficulty`, 보스 승리 시 해금. 사냥터 화면 난이도 바, DAY 라벨 표시, 박스 몬스터 렌더. 스모크 테스트 포트를 환경변수로 분리.
- 변경 파일: src/data.js, src/engine.js, src/render.js, src/app.js, style.css, tests/engine.test.mjs, tests/browser-smoke.mjs, HANDOFF.md, README.md, docs/WORKLOG.md.
- 검증: npm test 41개 통과, 격리 브라우저(4174/9224)에서 난이도 바·박스 몬스터 확인, browser-smoke 통과.

## 2026-09-11 · 모바일 터치 전환 1차 (Claude)

- 요청: 핸드폰으로 하는 게임. 모든 UI 터치 친화적. 규칙을 md에 녹일 것.
- 문서: `docs/MOBILE_UI.md`(기준 화면·탭 타깃 44px·호버 금지·Pointer Events·시트·검증 절차·감사 목록), AGENTS/HANDOFF/DESIGN/README에 원칙 반영.
- 구현: 몬스터 탭 툴팁 고정, 마을 편집 탭 미리보기→재탭 확정, 보스 소환·분해 확인 단계, title 의존 제거(스킬 비용·무게 페널티·배치 사유·빛기둥), `pointer:coarse` 탭 타깃 확대, `touch-action:manipulation`, 터치 문구 통일.
- 마을 메뉴: 기본 화면을 마을 창고(목록·상세·시설)로, '마을 편집' 버튼을 눌러야 꾸미기 모드. '편집 완료'로 복귀.
- 변경 파일: src/app.js, src/gear-ui.js, index.html, style.css, tests/browser-smoke.mjs, 문서 5종.
- 검증: npm test 41개 통과, node --check 통과. 브라우저 스모크·실기기 검증은 미완(로컬 Chromium 없음). 미커밋 상태.

## 2026-09-11 · 통합 인벤토리, 마을 제작소, 보스 20분 쿨다운·전리품, 전체 분해, 지도 아이콘 바

- 사용자 요청: 보스 20분에 한 번 소환, 하나의 인벤토리에 무기·방어구·장신구 구간, 배치 시 창고에서 제거, 등급별 전체 분해와 세트·전설 분해 특수 재료, 사용 불가 표시 제거, 제작은 마을 제작소에서만, 지도 바로가기는 마을·액트 아이콘으로 상단에.
- 구현: engine(쿨다운·bossLoot·salvageAll·relic 재료·정예 피해 보스 적용), items(보스 드롭표), gear-ui(hero/workshop 두 모드), app(제작소 뷰·존 아이콘 바·쿨다운 표시), index.html, style.css. 테스트 40개 통과, 스모크 테스트 갱신·통과.

## 2026-09-11 · 보스 스프라이트를 PixelLab 이미지로 교체 (Claude, feature/boss-sprite)

- 요청: PixelLab에서 만든 `pixellab-cute-wizard-1789113682677.png`(768×768, 256×256 셀 8방향 시트)로 보스 몬스터 교체.
- 정면 셀(유일하게 배경이 투명한 셀)만 잘라 2×2 평균 축소한 125×123 PNG를 `assets/boss.png`로 추가. 나머지 7방향 셀은 크림색 배경이 박혀 있어 사용하지 않음.
- `pixel-art.js bossSprite`: 이미지가 로드되면 이미지 프레임(1프레임은 몸통 1px 내려앉는 호흡)을, 아니면 기존 코드 스프라이트를 반환. 각 캔버스에 발 기준점 `anchorY` 부여.
- `render.js`: 보스 그림자·불씨·HP 바·이름 배지 위치를 스프라이트 크기 기준으로 계산. `server.mjs`에 PNG MIME 추가.
- 검증: `npm test` 40개 통과. 이 기기에 Chromium이 없어 브라우저 화면 확인은 미실시.

## 2026-09-11 · 지도 UI 정리, 배속 저장, 보스 쿨타임 초기화, 용사 선택 제거, 세트 보호 (Claude)

- 우하단 코너에 현재 위치 칩·소형 아이콘 바로가기(마을, ACT I~IV)·미니맵을 묶고, 헌터 추적·마을 편집·전체 지도 아이콘 삭제. 보스 소환은 좌상단으로 옮기고 쿨타임 카운트다운 표시, 클릭 시 남은 분×100 G로 초기화.
- 배속 1×/2×만, 저장에 보존. 일시정지 버튼 삭제(Alt+P 디버그 토글).
- 지도 선택 강조·지도 클릭 팝업·보드 칩 클릭·헤더 선택 용사 버튼 삭제. 마을 창고는 보기/분해 전용.
- 세트/유니크는 자동 분해 금지, 창고 부족 시 싼 장비 분해로 자리 확보, 불가하면 빛기둥 유지·만료 연장.
- 검증: npm test 45개, browser-smoke 통과(4174/9224).

## 2026-09-11 · 보스 확대·8방향 시트·암흑 선회 스킬, 레이드 중 소환 버튼 숨김 (Claude, feature/boss-spin)

- 요청: 보스가 너무 작다. 8방향으로 뽑은 시트를 써서 8방향으로 돌며 암흑 마법을 쓰는 스킬. 보스 체력 HUD와 "소환중" 버튼이 겹치니 레이드 중에는 버튼 숨김.
- `assets/boss.png`: 8셀 256px 시트 전체를 사용. 크림 배경은 가장자리 flood fill + 13px 이상 갇힌 배경색 영역 제거로 투명화, 256→192 면적 평균 축소, 발 정렬. 1536×192 가로 스트립.
- `pixel-art.js bossSprite(frame, facing)`: 스트립을 정사각 셀로 나눠 방향별 캔버스 캐시. `render.js`: `e.facing`으로 셀 선택, HP 바 폭을 스프라이트 폭의 절반으로, 선회 중 발밑 룬 링, `dark` 이펙트(보라 광선).
- `engine.js`: `FACING_VECTORS`, `facingTo`, `SPIN`(첫 9초, 쿨 18초, 0.3초×8칸, 사거리 230, 60° 원뿔, 공격력 55%, 약화 3초). 선회 중 보스는 이동·근접 공격 안 함. 저장 필드 `facing`, `spinCd`, `spin` 복원 검증.
- `app.js renderRaid`: 레이드 중 `#summon-boss` 숨김.
- 검증: npm test(선회 테스트 추가). 이 기기에 Chromium이 없어 화면 미확인.
## 2026-09-11 · 현재 버전 GitHub main 업로드

- 사용자 요청에 따라 기존 로컬 main 커밋을 원격 main에 push하는 작업 진행.
- 게임 코드 변경 없이 HANDOFF.md와 docs/WORKLOG.md에 업로드 범위·검증 기록 추가.
- `npm test`: 46개 전체 통과. 이번 작업에서 브라우저 스모크는 재실행하지 않음.
- 모바일 접속 주소: https://dave-nykim.github.io/DungeonMart/.

## 2026-09-18 · 단계별 보스 해금과 기본 2배속

- 요청: 노멀 1~10 자유 선택 제거, 기존 보스 처치 해금 규칙을 단계마다 적용. 모바일 보스 HP와 겹치는 1×/2× UI 제거, 기본 2배속.
- 구현: 노멀 1 보스 → 노멀 2 … 노멀 10 보스 → 나이트메어 1, 헬 10까지 동일 규칙. 이미 해금한 단계 재방문 가능, 변경 후 게임 시간 5분 잠금과 레이드 중 변경 금지 유지. `difficulty.unlockedStage` 추가. 구 저장은 현재 단계와 이미 해금된 하위 난이도를 보존하며, 아직 방문하지 않은 상위 단계는 순차 해금한다.
- 배속: 새 게임/구 저장 모두 speed=2로 변환, 프레임 시뮬레이션 2배 고정. 배속 DOM·렌더·클릭 처리 제거. 난이도 단계 잠금 및 안내를 모바일에서도 표시, 버튼 44px 이상.
- 변경 파일: src/engine.js, src/app.js, index.html, style.css, tests/engine.test.mjs, tests/browser-smoke.mjs, HANDOFF.md, docs/WORKLOG.md, docs/MOBILE_UI.md.
- 검증: `npm test` 47개 통과. 최종 해금 경계 조정 후 관련 테스트 3개 재통과. `node --check` 통과. 격리 Whale 9223에서 390×844·360×640 터치로 사냥터 진입, 단계 잠금·44px 타깃·가로 넘침 없음·보스 HP 표시·배속 UI 없음·저장 speed=2 확인. 스크린샷 `/tmp/dungeonmart-review/mobile-difficulty-390.png`, `mobile-difficulty-360.png`. 집중 검증 스크립트 `/tmp/dungeonmart-mobile-check.mjs`.
- 제한: 전체 `node tests/browser-smoke.mjs`는 기존 용사 추적 이동 assertion(61행)에서 실패하여 전체 통과 아님. 이번 변경 범위는 별도 모바일 검증 통과. 실제 폰 검증 미실시.
- 로컬 수정만, 커밋·push·배포하지 않음. 실행 `npm start`. 다음 작업: 전체 스모크의 용사 추적 검증 안정성 확인, 사용자 배포 지시 시 배포 후 실기기 확인.

## 2026-09-18 · 희귀 장비 전용 드롭·랜덤 제작·장비 도감

- 요청: 필드 장비 드롭은 세트·유니크만, 확률 대폭 하향. 일반·매직·레어는 제작 시 등급과 옵션 무작위. 후속 요청: 세트·유니크 도감 추가.
- 드롭 구현: `items.js DROP_TABLE/rollDropGrade`는 처치당 기본 확률 일반 몬스터 세트 0.002%+유니크 0.001%, 정예 0.08%+0.07%, 보스 1.5%+1%. 몬스터당 1회 판정, 실패 시 장비 없음. 장비 발견 옵션은 기존 최대 1.9배 유지. 첫 보스 유니크 확정·3회 보장·소프트 천장 제거(구 저장 pity 필드는 호환용으로 남지만 미사용). 해당 ilvl에 생성 가능한 세트/유니크가 없으면 일반 장비로 대체하지 않고 드롭 없음. 보석·각인석·재료 획득은 유지.
- 제작 구현: `craft(s, baseKey, tier=1, rng=Math.random)`, `craftCost(tier)`로 API 변경. 등급 선택 제거, 일반 60%·매직 30%·레어 10%. 일반은 기존 기본 성능/품질/홈 규칙, 매직·레어는 무작위 접사와 수치. 부적도 제작 등급 확률을 따른다. 비용은 기존 일반 제작 기준 철 8/20/40(1/2/3단), 제작대 제한 유지. 기존 보유·필드 장비 삭제 없음.
- 도감 구현: `src/equipment-codex.js` 신규. 하단 도감 → 몬스터/세트/유니크 3탭. 세트 5종·30부위의 구성·옵션·2~6부위 효과, 유니크 24종의 옵션·직업 제한·장비 레벨·획득 여부. 미획득도 공개. HTML details 터치로 펼침, 자동 갱신 후 펼침 유지. 수치는 1단 기준값이며 실제 드롭은 변동됨을 안내.
- 저장: v3 유지, `codex.setPieces`(세트ID:부위 → 1), 아이템 `codexRecorded` 추가. 입고/수동 획득/만료 자동 입고 시 기록, 분해 후에도 유지. 기존 창고·장착 장비에서 부위 기록 복원, 기존 유니크 기록 유지, 미획득 빛기둥 제외. 예전에 분해한 세트의 개별 부위는 구 저장에 정보가 없어 복원 불가(기존 codex.sets 최고 동시 착용 수는 그대로 보존).
- 변경 파일: src/items.js, src/engine.js, src/gear-ui.js, src/app.js, src/equipment-codex.js(신규), style.css, tests/items.test.mjs, tests/engine.test.mjs, tests/equipment-browser-smoke.mjs(신규), README.md, HANDOFF.md, docs/WORKLOG.md, docs/MOBILE_UI.md. 앞선 난이도·배속 수정도 같은 작업 트리에 미커밋 상태로 유지.
- 검증: `npm test` 49개 통과, 문법 검사·git diff --check 통과. 격리 Whale DevTools 9223에서 `node tests/equipment-browser-smoke.mjs` 통과: 390×844·360×640 실제 터치 입력으로 제작, 등급 선택 없음/확률 안내, 철 8 소모·장비 생성, 도감 5/24종·펼침 유지·획득 마이그레이션·44px 타깃·가로 넘침 없음·런타임 오류 없음. `/tmp/dungeonmart-review/mobile-craft-*.png`, `mobile-sets-*.png`, `mobile-uniques-*.png`.
- 실행: `npm start`. 신규 브라우저 테스트도 격리 9223 테스트 프로필 전용이며 테스트 저장을 덮어쓴다. 개인 브라우저 사용 금지. 전체 browser-smoke는 이번에 재실행하지 않았으며 이전 작업에서 용사 추적 assertion 실패 기록이 있다.
- 남은 일: 실제 폰 확인·드롭/경제 밸런스 플레이 피드백. 로컬 수정만 완료, 커밋·push·배포 미실시.

## 2026-09-18 · 창고 개수제·마을 통합·모바일 문구·메뉴 정리

- 요청: 창고는 크기 대신 아이템 1개당 1자리. 제작소를 마을 안 창고/제작소/꾸미기로 통합. 모바일 긴 설명 축약. 소식 탭 제거. 지도 아래 +/− 등 버튼 묶음 제거. 후속 질문: 제작 1/2/3단의 의미 설명(자동 단계 적용은 제안만 했으며 미구현).
- 구현: warehouseUsed는 warehouse.length. 제작·입고·장비 해제·희귀 장비 자리 확보 모두 1개 기준, UI 단위 개. 용량 80/100/120/144/168/192 유지. 저장 ID 목록·장비 보존, 저장 구조 변경 없이 기존 저장도 새 계산 적용. 용사 장착 격자 크기는 기존 규칙 유지.
- UI: 하단 맵/용사/사냥터/마을/도감/설정 6개. 마을 상단 창고/제작소/꾸미기 탭(44px 이상), 제작소 DOM을 마을 내부로 이동. 지도 대장간도 마을 제작소로 진입. 꾸미기에서 다른 탭으로 이동하면 편집 종료. 창고의 null 용사 참조 버그도 수정하여 장비 상세·분해 가능. 시설 개선은 제작소에서 제공.
- 모바일: 반복 설명·제작 완료 알림·버튼 문구 축약, 제작 입력칸 1열, 긴 장비명/상세 줄바꿈과 버튼 고정, 마을 탭 상단 고정. 소식 화면 DOM/렌더 제거, 내부 게임 로그 저장은 보존. 지도 확대/축소/배율/넓히기 UI와 핸들러 제거, 핀치·휠·지도 이동 유지.
- 변경 파일: src/engine.js, src/app.js, src/gear-ui.js, src/equipment-codex.js, index.html, style.css, tests/items.test.mjs, tests/browser-smoke.mjs, tests/equipment-browser-smoke.mjs, README.md 및 인계/작업/모바일 문서. 앞선 미커밋 작업 그대로 유지.
- 검증: 창고 크기 독립·80개 경계·제작 실패 시 재료 보존·장비 해제·세트 입고 시 1개만 분해·저장 왕복·100개 확장 테스트 추가. `npm test` 50개 전체 통과. 이후 UI 변경은 문법 검사·git diff --check 통과. 최종 `node tests/equipment-browser-smoke.mjs` 격리 Whale 9223에서 390×844·360×640 통과: 마을 3탭, 장비 상세, 개수 표기, 메뉴 제거, 44px 버튼/입력칸, 가로 넘침 없음, 실제 터치 핀치 확대, 제작·도감, 런타임 오류 없음. 화면 `/tmp/dungeonmart-review/mobile-craft-360.png` 등.
- 실행: npm start. 브라우저 테스트는 격리 테스트 저장을 덮어씀. 전체 browser-smoke는 기존 용사 추적 assertion 실패 이력이 있으며 이번에는 집중 검증만 실행. 실제 폰 확인 미실시.
- 로컬 수정만. 커밋/push/배포 미실시. 남은 논의: 제작 단계 자동 적용 여부는 사용자 결정 전이며 현행 1/2/3단 선택 유지.

## 2026-09-18 · 난이도 재조정: 무장비 나이트메어 보스 통과 방지

- 요청: 전체적으로 너무 쉬움. 장비 없이 레벨만 올려도 나이트메어 1 보스가 쉽다는 피드백.
- 원인: 보스 HP/공격에 stage가 반영되지 않음. 기존 보스는 파티 전투력×14와 평균 HP×0.2에 낮은 난이도 배율만 적용해 장비 성장도 보스 능력치에 상쇄됨. 일반 몬스터도 노멀 10의 성장폭이 3.7배에 그침.
- 구현: `data.js` 일반 몬스터 기본 HP 70→90, 공격 9→14. 단계 배율 `1.25^(stage-1)`(10단계 약 7.45배). 난이도 몬스터 배율 노멀1/나이트메어20/헬400. 액트간 비율·보상 배율·성장/장비 수치는 유지.
- 보스 `bossStats(s)`: 난이도 기본 HP/공격 = 노멀 12,000/90, 나이트메어 240,000/900, 헬 4,800,000/6,000. HP에 단계 배율×max(1,용사 수/5), 공격에 `1.16^(stage-1)`. 레벨/등급/장비와 무관하므로 장비 강화가 실제 이득. 단계별 증가와 난이도 경계 격차 적용. 전투 패턴·4분 제한 유지.
- 저장: v3 유지, `combatRevision=1` 추가. 구 저장의 현존 일반/정예/부하/보스 최대HP·공격을 새 규칙으로 환산하고 현재 HP 비율 보존. 용사/장비/재화/난이도 해금/레이드 남은 시간 보존, 새 기준 중복 적용 방지. 미래 revision 거부.
- 검증: npm test 53개 전체 통과. 보스 단계/파티수/레벨 독립성, 구 전투 저장 환산·HP 비율·재로드, Lv.60 무장비 5명 나이트메어1 실패 회귀 테스트 추가. node --check·git diff --check 통과.
- 시뮬레이션(일반 등급·미전직 Lv.60, 교전 위치에서 시작): 나이트메어1 무장비 5명 240초 제한 실패·보스 HP 약64% 남음, 직업별 세트 6부위 착용 5명은 약126.7초 처치. 무장비 20명도 실패·HP 약29% 남음. 비교 스크립트 `/tmp/dungeon-balance.mjs`. 이는 특정 구성 검증이며 모든 직업/스킬/옵션 조합의 완전한 밸런스 보장은 아님.
- 변경 파일: src/data.js, src/engine.js, tests/engine.test.mjs, HANDOFF.md, docs/WORKLOG.md, README.md. UI 구조 변경 없음, 브라우저 테스트는 이번 재조정에서 재실행하지 않음.
- 로컬만 반영, 커밋/push/배포 없음. 다음 작업: 실제 진행 구간별 피드백으로 나이트메어 후반·헬 수치 보정. 제작 단계 자동화는 여전히 제안만, 미구현.
